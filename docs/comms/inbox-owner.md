# 发给总负责人的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/owner-history.md`
- 当前消息处理完成后，要同步更新正式文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-29 23:55 T -> 总负责人

### 主题
`LLM V1` 首轮实现已落，但真实推理服务阻塞联调

### 背景

当前已完成以下工作：

1. 已落真实 `LLM` 环境变量配置读取
2. 已落最小 OpenAI 风格客户端
3. 已用真实 HTTP 调用替换关键词占位执行器
4. 已把结构化结果接到现有结果汇总、结果页和下载链路
5. 本地假 OpenAI 风格服务联调和自动化测试已通过

同时已对真实外部服务做探测：

- `/models` 可返回 `200`
- 但极小 `chat/completions` 请求在 `gpt-5.4-mini`、`gpt-5.2`、`gpt-5.4` 下均超时
- `SZCG2025000300-A` 真实样例上传和解析成功，但在审查执行阶段失败

### 问题

当前是否允许技术线程为联调目的临时切换到可稳定返回的模型或推理服务，继续完成真实端到端验证？

### 备选方案

1. 保持当前 `OPENAI_MODEL=gpt-5.4` 和现有网关不变，等待推理服务恢复后再继续联调
2. 允许联调阶段临时切到当前网关下可稳定返回的更轻模型，先完成 `LLM V1` 真实闭环验证
3. 提供新的可用推理网关或可用模型配置，再继续按当前实现联调

### 当前建议

建议优先选 `2` 或 `3`。

原因：

- 当前仓库侧实现和测试已基本就位
- 当前主阻塞不在仓库内，而在外部推理可用性
- 如果继续等待当前配置自然恢复，会阻塞 `LLM V1` 主线验收

### 期望回复

请总负责人拍板联调阶段是否允许临时切换模型或网关。
