# 发给总负责人的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/owner-history.md`
- 当前消息处理完成后，要同步更新正式文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-30 13:25 T -> 总负责人

### 主题
样例候选送审链路收口首包已完成，请确认是否进入真实 `LLM` 复跑与结果核对

### 背景
本轮已围绕真实样例 `SZCG2025000300-A` 完成候选送审链路收口首包，正式记录已写入 `docs/tech/v1/sample-risk-candidate-tuning-round-1.md`。

本轮已确认：

1. 当前规则候选排序已显式消费 `unit_label`
2. 当前已补高价值规则的业务单元优先软加分
3. 当前已补 `普通条款 / 普通表格行 / 不确定审查对象` 的轻量降权
4. 当前离线样例观察显示：
5. `R3` 已更稳定拿到 `原厂保修 / 培训方案` 等商务要求片段
6. `R5` 已更稳定拿到 `同品牌内窥镜主机 / 同品牌 30 度内窥镜` 等技术参数片段
7. `R12` 已更稳定拿到 `检测验证 / 违约责任 / 免费保修期` 等合同风险片段
8. `R9` 候选池已进一步收口到评分相关单元，但仍需真实结果复核

同时需要如实说明：

1. 当前只是完成候选送审链路收口首包
2. 当前还没有完成真实 `LLM` 复跑与最终结果核对
3. 因此当前不能把结论写成“关键风险已稳定进入最终报告”

### 问题
当前是否据此进入 `SZCG2025000300-A` 的受控真实 `LLM` 复跑与结果核对？

### 当前建议
建议立即进入 `SZCG2025000300-A` 的受控真实 `LLM` 复跑与结果核对。

原因：

1. 当前高价值候选片段进入送审链路的稳定性已经提升
2. 当前继续停留在离线观察口径，已经落后于实际主线
3. 下一步最有价值的动作，不再是继续补送审排序，而是直接验证最终结果里是否真的出现关键风险

### 期望回复
请总负责人确认是否据此进入 `SZCG2025000300-A` 的受控真实 `LLM` 复跑与结果核对。
