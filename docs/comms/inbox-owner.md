# 发给总负责人的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/owner-history.md`
- 当前消息处理完成后，要同步更新正式文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-30 20:25 T -> 总负责人

### 主题
`300-A` 轻量复跑下 `R4` 误报已压降，但真实 `R4` 召回仍不稳定，是否允许在同主题内继续做最后一层候选排序收口

### 背景
当前已完成 `R4` 首轮误报压制，并在更轻量配置下对 `SZCG2025000300-A` 执行了一次最小受控复跑。

本次复跑结果如下：

1. 任务 `task_0fe1f4bd81dc` 已进入 `completed`
2. 最终共有 `8` 组风险，规则分布为 `R12 = 1`、`R3 = 3`、`R5 = 3`、`R9 = 1`
3. `R4` 未进入最终结果
4. 当前程序性 `R4` 误报也未再进入最终结果

同时，离线候选核查确认：

1. `R4` 候选前列仍主要被 `资格性审查表 / 评审方法 / 综合评分法` 等说明条款占位
2. 真实条款 `投标人须具备深圳市医疗器械行业同类项目业绩不少于2个` 仍未稳定进入 `R4` 候选前列

### 问题
当前是否允许技术线程继续留在同一主题内，只做 `R4` 候选排序最后一层最小收口？

### 备选方案
1. 允许继续在同主题内补 `R4` 候选排序最小修正，不切新主题
2. 暂不再改实现，接受当前“误报先压下，但真实 `R4` 召回暂不稳定”的状态，等待后续统一处理

### 当前建议
建议选 `1`。

原因是当前问题已经很收敛，不需要扩第二轮能力补强，只需把真实业绩定向条款再往前顶一层即可。

### 期望回复
请总负责人拍板。
