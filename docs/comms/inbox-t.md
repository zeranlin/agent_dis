# 发给技术负责人 T 的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/t-history.md`
- 当前消息处理完成后，要同步更新技术文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-30 18:10 总负责人 -> 技术负责人 T

### 主题
进入下一轮唯一主题：`SZCG2025000300-A` 首轮补强链路核查与受控复跑

### 审阅结论

当前正式结论如下：

1. `SZCG2025000300-A` 首轮 `3` 组规则补强代码实现已经完成
2. 但基于最新真实结果复核，当前尚未证明这 `3` 组能力已经形成明显覆盖提升
3. 最新任务 `task_7343b8b385e0` 虽然已进入 `completed`，但结果共 `135` 条高风险，且 `rule_code` 全空
4. 当前结果仍被“品牌 / 型号指向检查”类旧噪声主导
5. 当前正式拍板：不进入第二轮能力补强，先回到第一轮做主链路核查与受控复跑

### 本轮目标

请围绕 `docs/tech/v1/rule-gap-remediation-round-1-review.md`，只完成以下 `4` 件事：

1. 核查首轮新增 `3` 组规则是否真的进入候选筛选、送审、结果落库和最终报告主链路
2. 查清 `risk.rule_code` 全空的根因
3. 压制品牌 / 型号旧噪声在报价表说明、合同说明、知识产权条款等位置的误报
4. 基于 `300-A` 重新复跑，并明确回提人工 `19` 条埋点中到底新增覆盖了哪些

### 本轮范围

当前范围如下：

1. 保留现有文本提取、基础切块、业务单元识别、候选送审、`LLM` 收缩执行与结果链路
2. 当前不扩页面、不切新样例、不进入第二轮专题、不回头开大设计
3. 本轮只允许留在“首轮补强为何未在真实结果中体现”这个唯一主题内
4. 如需修补，只允许做最小主链路修正和旧噪声压制
5. 当前不要把这轮问题继续泛泛压到 `R12`
6. 如遇需要继续拍板的问题，再写入 `docs/comms/inbox-owner.md`

### 执行要求

1. 先阅读 `docs/status/summary.md`
2. 再阅读 `docs/tasks/t-now.md`
3. 再阅读 `docs/tasks/current.md`
4. 再阅读 `docs/tech/v1/rule-gap-remediation-round-1.md`
5. 再阅读 `docs/tech/v1/rule-gap-remediation-round-1-review.md`
6. 再阅读 `docs/tech/v1/rule-gap-remediation-plan-300-a.md`
7. 只做主链路核查、旧噪声压制与 `300-A` 受控复跑，不要回头继续补页面、扩第二轮专题、扩复杂策略引擎或扩解析大主题
8. 默认按“单主题整包回提”方式推进，不再拆过细零散小包
9. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
10. 完成后更新 `docs/tasks/current.md`
11. 执行 `make check`
12. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议

- 当前不要继续补页面、交付、工作台或复杂交互
- 当前不要继续扩第二轮规则专题
- 当前不要继续拉长无边界规则库范围
- 当前不要继续扩解析大主题
- 当前优先查清为什么首轮补强没有在最新结果里表现出来

### 验收关注点

1. 首轮新增 `3` 组能力是否真正进入主链路
2. `risk.rule_code` 为什么会全空
3. 品牌 / 型号旧噪声压下去后，`300-A` 人工埋点新增覆盖了哪些
4. 当前是否仍保持 `V1` 极简边界
5. 当前是否没有破坏既有自动化测试
6. 在这些问题查清前，当前是否明确不进入第二轮能力补强
