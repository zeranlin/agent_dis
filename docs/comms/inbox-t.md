# 发给技术负责人 T 的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/t-history.md`
- 当前消息处理完成后，要同步更新技术文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-30 12:20 总负责人 -> 技术负责人 T

### 主题
进入下一轮唯一主题：政府采购审查对象解析扩展

### 审阅结论

当前正式结论如下：

1. 当前 `app/` 不需要推倒重来，现有文本提取、基础切块、`LLM` 收缩执行和结果链路都继续保留
2. 当前主缺口已经收口为：解析结果更像“通用文本块”，还不够像“政府采购审查对象”
3. 当前正式拍板：下一轮唯一主题切换为“政府采购审查对象解析扩展”
4. 当前不新建平行实现目录，后续实现统一继续落在 `app/`
5. 当前不回头继续补页面、零散稳定性补丁或旧的 `completed` 验证主题，先把输入骨架做对

### 本轮目标

请基于现有文本提取能力与基础切块能力，在 `app/` 内补齐“政府采购审查对象解析”最小可用实现，让解析结果能够稳定支持以下业务单元：

1. 资格条件
2. 资格性审查表
3. 符合性审查表
4. 评分项 / 评分表
5. 技术参数项
6. 商务要求项
7. 合同条款项
8. 政策适用条款

### 本轮范围

当前范围如下：

1. 保留现有文本提取能力，不回退 `PDF` / `DOCX` / `DOC` 最小解析入口
2. 保留现有基础切块能力，不删除已有 `section / clause / paragraph` 骨架
3. 在其上新增模块识别，至少稳定覆盖：
- 资格条件
- 资格性审查表
- 符合性审查表
- 评分办法 / 评分表
- 采购需求中的技术参数
- 采购需求中的商务要求
- 合同条款
- 政策适用条款
4. 在其上新增表格识别，重点吃透：
- 资格性审查表
- 符合性审查表
- 评分表
- 参数表
- 商务要求表
5. 在其上新增业务单元切分，让解析输出更接近审查对象，而不是普通文本块
6. 对标题不标准、结构不完整或边界不清的片段，补最小“不确定区补判”机制；优先采用轻量实现，必要时可先通过弱语义规则落地，避免一上来扩大 `LLM` 用量
7. 必须以真实样例 `SZCG2025000300-A` 作为主校准样例，验证上述八类对象是否能被稳定识别
8. 保持 V1 极简边界，不扩页面、不扩多文件、不扩复杂运营能力
9. 如遇需要继续拍板的问题，再写入 `docs/comms/inbox-owner.md`

### 执行要求

1. 先阅读 `docs/status/summary.md`
2. 再阅读 `docs/tasks/t-now.md`
3. 再阅读 `docs/business/v1/government-procurement-business-oriented-parsing-plan.md`
4. 再阅读 `docs/business/government-procurement-compliance-ontology.md`
5. 再阅读 `docs/business/government-procurement-review-framework.md`
6. 必要时再阅读 `docs/tasks/current.md`
7. 只做政府采购审查对象解析扩展，不要回头继续补提示词规则、页面、结果层补丁或旧的 `completed` 验证主题
8. 默认按“单主题整包回提”方式推进，不再拆过细零散小包
9. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
10. 完成后更新 `docs/tasks/current.md`
11. 执行 `make check`
12. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议

- 当前先不要继续补页面、交付、工作台或复杂交互
- 当前不要回到“连通性修复”旧主题
- 当前不要回头继续拉长提示词与规则包范围
- 当前不要继续在结果层堆补丁
- 当前不要把主题做成“大而全”的完整三级对象体系
- 当前优先把“政府采购审查对象”入口做稳，再让现有 `LLM` 收缩链路消费

### 验收关注点

1. 真实样例 `SZCG2025000300-A` 中，八类政府采购审查对象是否有稳定识别结果
2. 表格内容是否不再只是普通段落，而能进入更合理的业务单元
3. 解析输出字段是否仍能被现有 `LLM` 收缩执行链路消费
4. 当前实现是否仍保持 V1 极简边界
5. 当前是否没有破坏既有文本提取、基础切块和自动化测试
