# 发给技术负责人 T 的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/t-history.md`
- 当前消息处理完成后，要同步更新技术文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-30 04:10 总负责人 -> 技术负责人 T

### 主题
进入下一轮唯一主题：`completed` 验证

### 审阅结论

当前正式结论如下：

1. `LLM V1` 收缩方案首包与稳定性修复首包已经完成，并已形成批次级跑通证据
2. 当前已确认主线顺序应固定为：先做 `LLM` 收缩，再做 `completed` 验证，最后才做业务化解析优化
3. 当前正式拍板：进入第二步“`completed` 验证”
4. 当前不提前进入业务化解析优化主题
5. 当前不继续回头补新的零散稳定性补丁，先把正式验证做完

### 本轮目标

请围绕当前已完成的收缩执行与稳定性修复结果，完成“上传 -> `completed` -> 结果页 -> Markdown 下载”的正式验证整包，并回提是否可恢复当前阶段成果验证。

### 本轮范围

当前范围如下：

1. 使用真实样例 `SZCG2025000300-A` 做正式终态复跑
2. 使用补充正常样例 `DOCX` / `DOC` 做正式终态复跑
3. 必须验证上传 -> 等待 -> `completed` -> 结果页 -> Markdown 下载完整打通
4. 核对结果页、风险组、结论与下载结果是否一致
5. 保持现有页面、规则包和结果口径不扩散
6. 保持 V1 极简边界，不扩页面、不扩多文件、不扩复杂运营能力
7. 当前不提前进入业务化解析优化主题
8. 如遇需要继续拍板的问题，再写入 `docs/comms/inbox-owner.md`

### 执行要求

1. 先阅读 `docs/status/summary.md`
2. 再阅读 `docs/tasks/t-now.md`
3. 再阅读 `docs/tech/v1/llm-v1-acceptance-validation-round-1.md`
4. 必要时再阅读 `docs/tasks/current.md`
5. 只做 `completed` 验证，不要回头继续补提示词规则、结果层补丁、结构切分主题或新的零散稳定性补丁
6. 默认按“单主题整包回提”方式推进，不再拆过细零散小包
7. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
8. 完成后更新 `docs/tasks/current.md`
9. 执行 `make check`
10. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议

- 当前先不要继续补页面、交付、工作台或复杂交互
- 当前不要回到“连通性修复”旧主题
- 当前不要回头继续拉长提示词与规则包范围
- 当前不要继续在结果层或解析层堆补丁
- 当前优先把 `completed` 正式验证做完
- 当前业务化解析优化必须排在这一步之后

### 验收关注点

1. 真实样例 `SZCG2025000300-A` 是否稳定进入 `completed`
2. 补充正常样例 `DOCX` / `DOC` 是否稳定进入 `completed`
3. 结果页、风险组、结论与 Markdown 下载是否一致
4. 当前验证是否没有破坏失败页与上传拒绝页反馈
5. 当前实现是否仍保持 V1 极简边界
