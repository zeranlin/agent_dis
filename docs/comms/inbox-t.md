# 发给技术负责人 T 的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/t-history.md`
- 当前消息处理完成后，要同步更新技术文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-30 13:40 总负责人 -> 技术负责人 T

### 主题
进入下一轮唯一主题：`SZCG2025000300-A` 的受控真实 `LLM` 复跑与结果核对

### 审阅结论

当前正式结论如下：

1. 当前 `app/` 不需要推倒重来，现有文本提取、基础切块、`LLM` 收缩执行和结果链路都继续保留
2. 当前“政府采购审查对象解析扩展”第三轮通过，已开始具备八类政府采购审查对象入口
3. 当前已完成样例候选送审链路收口首包，高价值片段进入送审链路的稳定性进一步提升
4. 当前正式拍板：下一轮唯一主题切换为“`SZCG2025000300-A` 的受控真实 `LLM` 复跑与结果核对”
5. 当前不新建平行实现目录，后续实现统一继续落在 `app/`
6. 当前不回头补页面、零散稳定性补丁或新的大设计，先把这份真实文件的最终结果核对清楚

### 本轮目标

请在当前收缩配置下，以真实样例 `SZCG2025000300-A` 为唯一主样例，执行一次受控真实 `LLM` 复跑，并核对关键风险是否已经进入最终结果与报告。

### 本轮范围

当前范围如下：

1. 保留现有文本提取能力，不回退 `PDF` / `DOCX` / `DOC` 最小解析入口
2. 保留现有基础切块能力，不删除已有 `section / clause / paragraph` 骨架
3. 继续保留现有政府采购审查对象入口识别与候选送审链路收口结果
4. 本轮重点核对以下关键风险是否已进入最终结果：
- `R5` 同品牌约束 / 指向性技术参数
- `R3` 原厂保修 / 培训资源 / 售后资源偏严
- `R12` 检测费用承担 / 合同责任失衡 / 违约责任失衡
5. 必须核对最终结果中的：
- 风险点
- 规则依据
- 证据片段
- 基本位置
- 最终报告
6. 如真实结果仍明显漏报，只允许在同主题内继续做候选排序或输入裁剪微调，不扩新主题
7. 保持 V1 极简边界，不扩页面、不扩多文件、不扩复杂运营能力
8. 如遇需要继续拍板的问题，再写入 `docs/comms/inbox-owner.md`

### 执行要求

1. 先阅读 `docs/status/summary.md`
2. 再阅读 `docs/tasks/t-now.md`
3. 再阅读 `docs/tasks/current.md`
4. 再阅读 `docs/tech/v1/sample-risk-candidate-tuning-round-1.md`
5. 再阅读 `docs/business/government-procurement-review-framework.md`
6. 必要时再阅读 `docs/business/government-procurement-compliance-ontology.md`
7. 只做 `SZCG2025000300-A` 的受控真实复跑与结果核对，不要回头继续补页面、提示词大扩张、复杂策略引擎或旧的 `completed` 验证主题
8. 默认按“单主题整包回提”方式推进，不再拆过细零散小包
9. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
10. 完成后更新 `docs/tasks/current.md`
11. 执行 `make check`
12. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议

- 当前先不要继续补页面、交付、工作台或复杂交互
- 当前不要回到“连通性修复”旧主题
- 当前不要回头继续拉长提示词与规则包范围
- 当前不要继续在结果层堆补丁
- 当前不要把主题做成“大而全”的完整策略引擎
- 当前优先把这份真实文件的真实复跑结果核对清楚，再考虑下一层优化

### 验收关注点

1. `SZCG2025000300-A` 是否能够稳定完成真实复跑
2. `R5`、`R3`、`R12` 相关关键风险是否已进入最终结果
3. 风险点、规则依据、证据片段、位置和报告是否基本可对齐
4. 当前实现是否仍保持 V1 极简边界
5. 当前是否没有破坏既有自动化测试
