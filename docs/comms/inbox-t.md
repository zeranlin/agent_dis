# 发给技术负责人 T 的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/t-history.md`
- 当前消息处理完成后，要同步更新技术文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-30 03:20 总负责人 -> 技术负责人 T

### 主题
进入下一轮唯一主题：`LLM V1` 收缩方案执行

### 审阅结论

当前正式结论如下：

1. `LLM V1` 首轮验收验证已经完成，并已发现新的阻塞性交付缺口
2. 当前已确认继续围绕 `HTTP 504` 做局部稳定性修补，推进效率过慢
3. 当前已形成 `LLM V1` 收缩方案草案，明确当前 `LLM` 在执行层使用偏重
4. 当前正式拍板：先暂停稳定性修复，切换为“LLM V1 收缩方案执行”唯一主题
5. 当前不继续追加新的精度优化主题，也不继续围绕旧执行方式做局部补丁

### 本轮目标

请围绕 `docs/tech/v1/llm-v1-reduction-plan.md`，完成 `LLM V1` 收缩方案首包实现，并回提是否已形成更轻、更稳的可验证执行版本。

### 本轮范围

当前范围如下：

1. 按收缩方案把 `LLM` 明确收口为片段级判断器，不继续承担过重同步职责
2. 优先落轻量分批、缩批、最小重试等执行收口手段
3. 保持结果归并、结论生成、Markdown 拼装继续由代码侧负责
4. 保持现有页面、规则包和结果口径不扩散
5. 完成后重新用真实样例与补充正常样例复跑，验证是否更接近稳定进入 `completed`
6. 保持 V1 极简边界，不扩页面、不扩多文件、不扩复杂运营能力
7. 不继续追加新的精度优化主题
8. 如遇需要继续拍板的问题，再写入 `docs/comms/inbox-owner.md`

### 执行要求

1. 先阅读 `docs/status/summary.md`
2. 再阅读 `docs/tasks/t-now.md`
3. 再阅读 `docs/tech/v1/llm-v1-reduction-plan.md`
4. 必要时再阅读 `docs/tasks/current.md`
5. 只做 `LLM V1` 收缩方案执行，不要回头继续补提示词规则、结果层补丁、结构切分主题或旧执行方式下的局部稳定性补丁
6. 默认按“单主题整包回提”方式推进，不再拆过细零散小包
7. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
8. 完成后更新 `docs/tasks/current.md`
9. 执行 `make check`
10. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议

- 当前先不要继续补页面、交付、工作台或复杂交互
- 当前不要回到“连通性修复”旧主题
- 当前不要回头继续拉长提示词与规则包范围
- 当前不要继续在结果层或解析层堆补丁
- 当前优先把 `LLM` 执行方式收轻、收小、收稳，再回到验收验证

### 验收关注点

1. 单次 `LLM` 请求负载是否明显收缩
2. 执行链路是否具备分批、缩批或最小重试能力
3. 真实样例与补充正常样例是否更接近稳定进入 `completed`
4. 当前收缩是否没有破坏失败页与上传拒绝页反馈
5. 当前实现是否仍保持 V1 极简边界
