# 发给技术负责人 T 的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/t-history.md`
- 当前消息处理完成后，要同步更新技术文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-29 23:20 总负责人 -> 技术负责人 T

### 主题
停止继续下钻设计，正式切回 `LLM V1` 首轮实现

### 审阅结论

当前正式结论如下：

1. 页面版最小闭环已经通过复审，可以继续沿用现有上传页、等待页、结果页和下载链路
2. 当前仓库的真实主线不是继续做页面或交付支持，而是切回 `LLM` 高精度审查执行
3. 当前关于 `LLM` 主流程、输入模型、输出模型、分片策略、风险归并和结论输出，已经有足够的方案骨架
4. 现在不再继续下钻 `8.x / 9.x` 设计，直接进入 `LLM V1` 首轮实现

### 本轮目标

请正式结束“待命支持 / 页面收口”主题，进入 `LLM V1` 首轮实现，目标是用真实 `LLM` 调用替换当前关键词占位执行层，打通最小可用的 `LLM` 审查闭环。

### 本轮范围

当前范围如下：

1. 接入真实 `LLM` 调用配置，使用环境变量读取模型服务地址、模型名称和鉴权信息
2. 落一个最小 `LLM` 客户端或适配层，先打通单次调用
3. 用真实分片审查替换当前关键词占位执行器
4. 让 `LLM` 输出能够进入现有结果汇总、结果页与下载链路
5. 保持 V1 极简边界，不扩页面、不扩多文件、不扩复杂运营能力
6. 如遇需要拍板的问题，再写入 `docs/comms/inbox-owner.md`

### 执行要求

1. 先阅读 `docs/status/summary.md`
2. 再阅读 `docs/tasks/t-now.md`
3. 再阅读 `docs/tech/v1/llm-v1-implementation-kickoff.md`
4. 必要时再阅读 `docs/tasks/current.md`
5. 先完成最小 `LLM` 连通验证，再进入执行链路替换
6. 默认按“单主题整包回提”方式推进，不再拆过细零散小包
7. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
8. 完成后更新 `docs/tasks/current.md`
9. 执行 `make check`
10. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议

- 当前先不要继续补页面、交付、工作台或复杂交互
- 当前先不要继续深挖 `8.x / 9.x` 更下一层方案
- 当前优先把“真实 `LLM` 调用 -> 结构化风险输出 -> 现有结果链路接轨”打通
- 优先保证能跑通，再逐步提高精度和结构完整度

### 验收关注点

1. 是否已真实接入 `LLM`，而不是继续走关键词占位
2. 结构切分后的输入是否已能送给 `LLM`
3. `LLM` 输出是否已能进入现有结果汇总、结果页和下载链路
4. 当前实现是否仍保持 V1 极简边界
