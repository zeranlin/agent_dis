# 发给技术负责人 T 的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/t-history.md`
- 当前消息处理完成后，要同步更新技术文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-30 02:45 总负责人 -> 技术负责人 T

### 主题
进入下一轮唯一主题：真实 `LLM` 审查执行稳定性修复

### 审阅结论

当前正式结论如下：

1. `LLM V1` 首轮验收验证已经完成，并已发现新的阻塞性交付缺口
2. 当前已确认正常样例无法稳定进入 `completed`
3. 当前已确认核心阻塞集中在真实 `LLM` 审查执行阶段 `HTTP 504`
4. 当前正式拍板：切回“真实 LLM 审查执行稳定性修复”唯一主题
5. 当前不继续追加新的精度优化主题，也不继续停留在验收结论争论

### 本轮目标

请围绕真实 `LLM` 审查执行阶段 `HTTP 504` 阻塞，完成最小稳定性修复整包，并回提是否已恢复正常样例进入 `completed`。

### 本轮范围

当前范围如下：

1. 优先排查联调服务与当前调用参数组合下的超时原因
2. 优先尝试最小稳定性修复手段，例如超时参数、最小重试、缩小单次请求负载或缩批
3. 保持现有页面、结果口径和规则包不扩散
4. 修复后重新使用真实样例与补充正常样例复跑
5. 必须验证上传 -> 等待 -> `completed` -> 结果页 -> Markdown 下载重新打通
6. 保持 V1 极简边界，不扩页面、不扩多文件、不扩复杂运营能力
7. 不继续追加新的精度优化主题
8. 如遇需要继续拍板的问题，再写入 `docs/comms/inbox-owner.md`

### 执行要求

1. 先阅读 `docs/status/summary.md`
2. 再阅读 `docs/tasks/t-now.md`
3. 再阅读 `docs/tech/v1/llm-v1-acceptance-validation-round-1.md`
4. 必要时再阅读 `docs/tasks/current.md`
5. 只做真实 `LLM` 审查执行稳定性修复，不要回头继续补提示词规则、结果层补丁或结构切分主题
6. 默认按“单主题整包回提”方式推进，不再拆过细零散小包
7. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
8. 完成后更新 `docs/tasks/current.md`
9. 执行 `make check`
10. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议

- 当前先不要继续补页面、交付、工作台或复杂交互
- 当前不要回到“连通性修复”旧主题
- 当前不要回头继续拉长提示词与规则包范围
- 当前不要继续在结果层或解析层堆补丁
- 当前优先把“正常样例稳定进入 completed”恢复出来，再回到验收验证

### 验收关注点

1. 真实样例 `SZCG2025000300-A` 是否重新稳定进入 `completed`
2. 补充正常样例 `DOCX` / `DOC` 是否重新稳定进入 `completed`
3. `completed` 结果页与 Markdown 下载是否重新恢复
4. 当前修复是否没有破坏失败页与上传拒绝页反馈
5. 当前实现是否仍保持 V1 极简边界
