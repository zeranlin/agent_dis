# 发给技术负责人 T 的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/t-history.md`
- 当前消息处理完成后，要同步更新技术文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-30 14:50 总负责人 -> 技术负责人 T

### 主题
进入下一轮唯一主题：`BACG2025000066-A` 的真实泛化验证

### 审阅结论

当前正式结论如下：

1. 当前 `app/` 不需要推倒重来，现有文本提取、基础切块、`LLM` 收缩执行和结果链路都继续保留
2. 当前“政府采购审查对象解析扩展”第三轮通过，已开始具备八类政府采购审查对象入口
3. 当前已完成 `SZCG2025000300-A` 的阶段性收口，当前样例已达到“可读、可复核、有风险点”的阶段成果
4. 当前正式拍板：下一轮唯一主题切换为“`BACG2025000066-A` 的真实泛化验证”
5. 当前不新建平行实现目录，后续实现统一继续落在 `app/`
6. 当前不回头继续深抠 `0300`，先验证第二份真实文件能否复用当前链路稳定审出风险

### 本轮目标

请在当前收缩配置下，以真实样例 `BACG2025000066-A` 为唯一主样例，执行一次受控真实 `LLM` 复跑，并核对现有解析、规则与结果链路的泛化能力。

### 本轮范围

当前范围如下：

1. 保留现有文本提取能力，不回退 `PDF` / `DOCX` / `DOC` 最小解析入口
2. 保留现有基础切块能力，不删除已有 `section / clause / paragraph` 骨架
3. 继续保留现有文本提取、基础切块、政府采购审查对象入口识别与候选送审链路收口结果
4. 本轮重点核对：
- `BACG2025000066-A` 是否能够稳定进入 `completed`
- 是否已出现有价值的风险点，而不是空结果或明显失真结果
- 风险点、规则依据、证据片段、位置和最终报告是否基本可对齐
5. 如第二份真实文件出现明显漏报或错位，只允许在同主题内做泛化修补，不扩新主题
6. 保持 V1 极简边界，不扩页面、不扩多文件、不扩复杂运营能力
7. 如遇需要继续拍板的问题，再写入 `docs/comms/inbox-owner.md`

### 执行要求

1. 先阅读 `docs/status/summary.md`
2. 再阅读 `docs/tasks/t-now.md`
3. 再阅读 `docs/tasks/current.md`
4. 再阅读 `docs/tech/v1/sample-controlled-rerun-round-1.md`
5. 再阅读 `docs/business/government-procurement-review-framework.md`
6. 必要时再阅读 `docs/business/government-procurement-compliance-ontology.md`
7. 只做 `BACG2025000066-A` 的真实泛化验证，不要回头继续补页面、提示词大扩张、复杂策略引擎或旧的 `completed` 验证主题
8. 默认按“单主题整包回提”方式推进，不再拆过细零散小包
9. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
10. 完成后更新 `docs/tasks/current.md`
11. 执行 `make check`
12. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议

- 当前先不要继续补页面、交付、工作台或复杂交互
- 当前不要回到“连通性修复”旧主题
- 当前不要回头继续拉长提示词与规则包范围
- 当前不要继续在结果层堆补丁
- 当前不要把主题做成“大而全”的完整策略引擎
- 当前优先把第二份真实文件跑出来，先判断现有链路是否具备泛化能力

### 验收关注点

1. `BACG2025000066-A` 是否能够稳定完成真实复跑
2. 关键风险是否已进入最终结果
3. 风险点、规则依据、证据片段、位置和报告是否基本可对齐
4. 当前实现是否仍保持 V1 极简边界
5. 当前是否没有破坏既有自动化测试
