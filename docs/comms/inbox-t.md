# 发给技术负责人 T 的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/t-history.md`
- 当前消息处理完成后，要同步更新技术文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-29 01:28 总负责人 -> 技术负责人 T

### 主题
补解析链路首轮实现准备的 2 个最小缺口

### 审阅结论

你本轮提交的“解析链路可用化首轮实现准备”方向正确，但当前还不能放行进入下一轮解析链路深化。

当前结论如下：

1. 当前真实解析入口、轻清洗、切分块对象和失败分类方向是对的
2. 但当前还存在 2 个会影响主链路正确性的最小缺口
3. 需要先补齐这 2 个缺口，再进入下一轮解析链路深化

### 本轮目标

请先补以下 2 个问题：

1. 修正 `1.1` / `1.2` 等条款行被误判为章节的问题
2. 修正“只有章节标题、没有可审查片段”的文件被错误推进到审查队列的问题

### 本轮范围

当前范围如下：

1. 收紧章节判定逻辑，避免吞掉多级编号条款
2. 必要时调整章节 / 条款判定顺序，确保切分结构符合已收口产品口径
3. 在解析阶段增加“至少生成一个非 `section` 的可审查片段”校验
4. 若未形成可审查片段，应在解析阶段直接失败，不要推进到 `review_queued`
5. 补对应最小测试，覆盖这 2 个问题
6. 如有必要，补一版短文档说明本轮修正点

### 执行要求

1. 先阅读 `docs/status/summary.md`
2. 再阅读 `docs/tasks/t-now.md`
3. 必要时再阅读 `docs/tasks/current.md`
4. 重点对照 `docs/business/v1/parsing-and-segmentation-minimum.md`
5. 直接补代码与测试
6. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
7. 完成后更新 `docs/tasks/current.md`
8. 执行 `make check`
9. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议

- 这轮只补主链路正确性问题，不展开新的解析精度优化主题
- 不提前发散到 OCR、复杂表格、复杂版式恢复或更重编排

### 验收关注点

1. `1.1` / `1.2` 这类条款行是否不再被误判为章节
2. 切分结果是否与当前产品口径一致
3. 无可审查片段的文件是否会在解析阶段直接失败
4. 失败分类是否仍停留在解析阶段而不是漂到审查阶段
