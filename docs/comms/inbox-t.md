# 发给技术负责人 T 的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/t-history.md`
- 当前消息处理完成后，要同步更新技术文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-30 02:20 总负责人 -> 技术负责人 T

### 主题
进入下一轮唯一主题：`LLM V1` 验收验证

### 审阅结论

当前正式结论如下：

1. 三个优先级主题首包已经全部完成，并已完成真实样例验证
2. 当前已确认第三优先级主题把真实样例原始命中总数由 `18` 压降到 `9`
3. 当前已确认当前版本不存在新的阻塞性交付缺口
4. 当前正式拍板：精度优化主线阶段性收口，下一轮进入 `LLM V1` 验收验证
5. 当前不继续追加新一轮精度优化主题

### 本轮目标

请围绕当前页面闭环、真实样例与下载链路，完成 `LLM V1` 验收验证整包，并回提是否可作为当前阶段成果进行验证。

### 本轮范围

当前范围如下：

1. 准备并使用当前真实样例与至少一份补充样例做验收验证
2. 逐个走通上传页 -> 等待页 -> 结果页 -> Markdown 下载
3. 核对任务状态、页面状态、最终结论、风险组展示和报告下载
4. 核对失败场景是否仍保持正确反馈
5. 输出“可进入当前阶段成果验证”或“仍存在阻塞性交付缺口”的整包结论
6. 保持 V1 极简边界，不扩页面、不扩多文件、不扩复杂运营能力
7. 不继续追加新的精度优化主题
8. 如遇需要继续拍板的问题，再写入 `docs/comms/inbox-owner.md`

### 执行要求

1. 先阅读 `docs/status/summary.md`
2. 再阅读 `docs/tasks/t-now.md`
3. 再阅读 `docs/tech/v1/long-clause-splitting-round-1.md`
4. 必要时再阅读 `docs/tasks/current.md`
5. 只做 `LLM V1` 验收验证，不要回头继续补提示词规则、结果层补丁或结构切分主题
6. 默认按“单主题整包回提”方式推进，不再拆过细零散小包
7. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
8. 完成后更新 `docs/tasks/current.md`
9. 执行 `make check`
10. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议

- 当前先不要继续补页面、交付、工作台或复杂交互
- 当前不要回到“连通性修复”旧主题
- 当前不要回头继续拉长提示词与规则包范围
- 当前不要继续在结果层或解析层堆补丁
- 当前优先把“真实文件可验证 + 页面可演示 + 报告可交付”这一轮验收做扎实

### 验收关注点

1. 上传页、等待页、结果页是否完整可用
2. 真实文件审查后，结果页与 Markdown 报告是否可读
3. 当前风险组、结论和证据展示是否具备验证价值
4. 失败场景反馈是否仍然正确
5. 当前实现是否仍保持 V1 极简边界
