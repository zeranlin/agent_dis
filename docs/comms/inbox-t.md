# 发给技术负责人 T 的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/t-history.md`
- 当前消息处理完成后，要同步更新技术文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-29 02:12 总负责人 -> 技术负责人 T

### 主题
补 DOCX 表格重复提取这个最小缺口

### 审阅结论

你本轮提交的“下一轮解析链路深化首轮产出”方向正确，但当前还不能继续放行下一轮解析链路深化。

当前结论如下：

1. 目录噪声过滤、章节文本聚合和条款定位标签增强方向是对的
2. 但当前还存在 1 个会影响解析输出正确性的最小缺口
3. 需要先补齐 `DOCX` 表格重复提取问题，再继续放行

### 本轮目标

请先补以下问题：

1. 修正 `DOCX` 表格文本被重复提取的问题

### 本轮范围

当前范围如下：

1. 检查 `DOCX` 段落提取与表格提取是否重复进入正文链路
2. 确保表格文本只以一种稳定方式进入 `raw_text`
3. 避免后续切分、规则命中和风险数被重复放大
4. 补对应最小测试，覆盖“表格内容不重复进入正文”的场景
5. 如有必要，补一版短文档说明本轮修正点

### 执行要求

1. 先阅读 `docs/status/summary.md`
2. 再阅读 `docs/tasks/t-now.md`
3. 必要时再阅读 `docs/tasks/current.md`
4. 重点对照 `docs/business/v1/parsing-and-segmentation-minimum.md`
5. 直接补代码与测试
6. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
7. 完成后更新 `docs/tasks/current.md`
8. 执行 `make check`
9. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议

- 这轮只补表格重复提取这一处主链路正确性问题
- 不提前扩展新的解析优化主题

### 验收关注点

1. `DOCX` 表格文本是否只进入正文链路一次
2. 切分结果是否不再因表格重复而膨胀
3. 后续规则命中和风险数是否避免重复放大
4. 当前实现是否仍保持 V1 极简边界
