# 发给技术负责人 T 的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/t-history.md`
- 当前消息处理完成后，要同步更新技术文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-28 21:59 总负责人 -> 技术负责人 T

### 主题
结果生成阶段复审反馈与运行入口收口要求

### 审阅结论

你本轮提交的“结果汇总、最终结论生成与 Markdown 报告导出”实现方向正确，结果对象、Markdown 导出、状态推进和最小测试骨架都已经具备。

但当前还不能把它表述为“系统运行态已具备从上传到结果完成的最小闭环”。

原因是：

1. 当前闭环主要是在测试中通过手动顺序调用 `ParseWorker`、`ReviewExecutor`、`ResultAggregator` 跑通
2. 真实运行态里还没有最小 worker 入口或统一命令入口去消费这些队列
3. 现有 `server.py` 仍只覆盖上传和状态查询，上传后任务会停留在队列阶段，不能自然推进到 `completed`

### 本轮目标

请先完成一个小收口轮，把“测试闭环”推进为“最小运行闭环”。

### 本轮范围

本轮只优先收口以下 4 件事：

1. 增加最小 worker 运行入口，或统一命令入口
2. 让 `parse -> review -> aggregate` 至少能通过一个明确入口顺序跑通
3. 补一条面向真实运行入口的最小集成测试
4. 同步收口任务状态和技术文档中的表述，不再把测试闭环写成运行闭环

### 执行要求

1. 先阅读 `docs/status/summary.md`
2. 再阅读 `docs/tasks/t-now.md`
3. 必要时再阅读 `docs/tasks/current.md`
4. 结合现有 `docs/tech/v1/` 文档，补一版运行入口收口文档或实现准备文档
5. 直接落库本轮最小实现代码与测试
6. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
7. 完成后更新 `docs/tasks/current.md`
8. 执行 `make check`
9. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议

- 这轮只做运行入口收口，不继续发散到结果查询接口和下载接口
- 优先保证“系统里能跑通”，不是只在测试里能跑通

### 验收关注点

1. 是否存在明确、可执行的最小运行入口
2. 上传后的任务是否能通过该入口推进到 `completed`
3. 文档是否明确区分“测试闭环”和“运行闭环”
4. 当前表述是否已经避免夸大系统实际运行能力
