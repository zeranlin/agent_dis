# 发给技术负责人 T 的收件箱

## 使用说明

- 总负责人或产品经理 P 若有技术方案、接口、状态、实现准备相关问题，可追加写在本文件
- 技术负责人 T 处理后，可在原条目下继续追加回复
- 若已形成正式结论，应同步更新技术文档和 `docs/tasks/current.md`

## 当前消息

## 2026-03-28 15:30 总负责人 -> 技术负责人 T

### 主题
进入实现准备第一轮

### 背景
当前产品方案、交接包、技术方案首包、协作机制和通信机制都已形成，仓库状态已具备从方案阶段进入实现准备阶段的条件。

### 问题
请你按当前仓库状态继续推进，进入实现准备第一轮。

### 本轮范围
本轮只聚焦以下 4 件事：

1. 上传接口
2. 任务状态模型
3. 文件解析链路
4. 规则资产加载

### 执行要求

1. 先阅读 `docs/tasks/current.md`
2. 先阅读 `docs/tech/v1/` 下现有技术文档
3. 先总结当前实现准备边界和本轮目标
4. 再直接落库本轮技术文档或实现准备文档
5. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
6. 完成后更新 `docs/tasks/current.md`
7. 执行 `make check`
8. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议
优先把“能让开发真正开始动起来”的最小实现准备物先补齐，不要在这一轮继续发散到更多扩展主题。

### 期望回复
请读取后直接开始推进，并将本轮产出写回仓库。

## 2026-03-28 16:10 总负责人 -> 技术负责人 T

### 主题
技术方案审阅反馈与本轮收口要求

### 背景
总负责人已审阅你最新提交的技术方案文档和最小代码骨架，包括 `docs/tech/v1/` 下文档以及当前 `app/`、`tests/` 中已落地实现。

### 审阅结论
当前技术方案整体方向正确，系统分层、核心模块边界和主链路已经基本具备继续开发基础。

但这轮不建议直接无条件进入下一轮扩展实现，需先收口以下问题。

### 需优先处理的问题

1. `JsonRepository` 当前存在并发写覆盖风险  
   当前服务使用多线程 HTTP Server，但任务和文档元数据写入采用整文件读写，缺少最小并发保护或原子写策略。请优先补最小保护方案，避免任务丢失、状态错乱或 JSON 文件损坏。

2. `created -> upload_validated` 的状态口径需要统一  
   当前文档把上传时序定义为先写 `created`，再更新到 `upload_validated`，但现有实现直接以 `upload_validated` 作为初始内部状态。请统一文档与实现，不要让状态机定义和代码实现长期漂移。

3. `architecture-layering.md` 的 Mermaid 图需要收口到仓库规范  
   当前该文档里的流程图未按现行 Mermaid 规范执行。请按仓库规范修正，避免技术文档标准继续漂移。

### 本轮要求

请先做一个小收口轮，只处理上述三个问题：

1. 并发写保护
2. 状态机口径统一
3. Mermaid 规范修正

### 执行要求

1. 先读取本条消息
2. 直接修改相关代码与文档
3. 更新 `docs/tasks/current.md`
4. 执行 `make check`
5. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议
这轮先收稳定性和一致性，不要继续往解析 worker、规则资产加载器之外发散。

### 期望回复
请完成这轮收口后，再继续下一步实现准备。

## 2026-03-28 16:40 总负责人 -> 技术负责人 T

### 主题
上传异常失败态补充收口

### 背景
总负责人已审阅你完成第一轮技术收口后的最新技术方案与代码实现。并发写保护、状态口径统一和 Mermaid 规范修正这三项已基本收口通过。

### 审阅结论
当前最小服务骨架已经更稳定，可以继续往输入链路推进。

但在进入下一轮实现前，还需要补一个很小的失败态收口。

### 需补充的问题

当前上传链路先写入 `created` 任务，再执行文件存储和文档元数据落地。  
如果 `save_upload` 或 `save_document` 过程中发生异常，请求会直接失败，但仓库里会残留停在 `created` 的任务，这与当前技术文档中“存储失败应置为 failed”的口径不一致。

### 本轮要求

请补一个小修正轮，只处理以下 3 件事：

1. 为上传链路增加异常兜底
2. 在存储或元数据落地失败时，将任务置为 `failed`
3. 补一条对应测试，验证失败态能够被正确记录

### 执行要求

1. 先读取本条消息
2. 直接修改相关代码与测试
3. 如文档口径需要同步收口，一并更新文档
4. 更新 `docs/tasks/current.md`
5. 执行 `make check`
6. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议
这轮还是小修正，不要在同一轮里继续扩展到解析 worker 或规则资产加载之外的新主题。

### 期望回复
请完成这个失败态补充收口后，再继续下一步实现准备。

## 2026-03-28 17:10 总负责人 -> 技术负责人 T

### 主题
进入下一轮实现准备

### 背景
总负责人已完成对你最新技术方案和代码实现的复审。当前最小上传链路已完成第一轮稳定性收口，上传失败态问题也已补齐。

### 审阅结论
当前阶段已可以放行进入下一轮实现准备。

### 本轮目标

请继续推进以下两个主题：

1. 解析 worker
2. 规则资产加载器

目标是让链路真正实现：

`upload_validated -> parsing -> parsed`

### 本轮范围

优先收口以下内容：

1. 解析任务消费方式
2. `PDF` / `Word` 解析器最小实现
3. `procurement_document`、`document_chapter`、`document_clause` 的最小落地
4. 默认规则包和固定任务指令的资产目录与加载器
5. 对应状态推进与最小测试

### 执行要求

1. 先阅读 `docs/tasks/current.md`
2. 先阅读 `docs/tech/v1/implementation-prep-round-1.md`
3. 直接落库本轮技术文档和实现
4. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
5. 完成后更新 `docs/tasks/current.md`
6. 执行 `make check`
7. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议
这一轮仍然优先保证输入链路跑通，不要提前展开审查执行器细节调优和结果汇总扩展。

### 期望回复
请完成本轮产出后，再回到总负责人处接受复审。

## 2026-03-28 17:40 总负责人 -> 技术负责人 T

### 主题
解析能力与规则包格式口径补充收口

### 背景
总负责人已完成对你最新一轮“解析 worker + 规则资产加载器”实现的复审。当前输入链路框架已明显前进，状态推进、队列消费和资产加载最小骨架都已落地。

### 审阅结论
这轮实现可以视为“输入链路框架已跑通”，但还不建议直接把“真实支持 PDF / Word”作为已完成结论写死。

### 需补充处理的问题

1. 真实 `PDF` / `Word` 解析能力与当前实现口径不一致  
   当前解析 worker 实际上是按字节直接做文本解码，测试样本也是纯文本伪装文件。这可以作为最小占位实现，但不能等同于真实 `PDF` / `Word` 解析能力。请明确收口：
   - 是先把当前实现标注为“最小文本解码占位”
   - 还是补上至少一种真实文件提取方案

2. 规则包文件格式与加载器口径不一致  
   当前文件名是 `.yaml`，但内容实际写成 JSON，加载器也使用 `json.loads`。请统一为一种稳定口径，避免后续维护时出现“按 YAML 编辑但加载器按 JSON 解析”的问题。

### 本轮要求

请先做一个小收口轮，只处理以下 2 件事：

1. 统一真实文件解析能力的当前口径
2. 统一规则包文件格式与加载器实现口径

### 执行要求

1. 先读取本条消息
2. 直接修改相关代码与文档
3. 若选择暂不补真实 PDF / Word 提取，请在技术文档和任务状态中明确标注“当前仅为占位实现”
4. 更新 `docs/tasks/current.md`
5. 执行 `make check`
6. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议
这轮优先做“口径收口”，不要让产品侧误以为真实 PDF / Word 能力已经完成。

## 2026-03-28 21:41 总负责人 -> 技术负责人 T

### 主题
进入结果汇总、最终结论生成与报告导出最小实现阶段

### 背景
总负责人已完成对你最新一轮“审查输入装配与审查执行最小骨架”的复审。当前系统已经具备以下基础：

- 上传与任务状态链路已可运行
- 解析 worker 与规则资产加载器已落地
- 审查输入装配器已可组装运行时输入
- 审查执行最小骨架已可生成风险点与证据片段中间对象

### 审阅结论
当前阶段可以放行进入“结果生成”主流程。

但这里仍要强调：本轮目标不是做完整产品化结果中心，而是补齐 V1 最小闭环中“结果汇总 -> 最终结论 -> Markdown 报告导出”这条主链路。

### 本轮目标

请继续推进以下 4 个主题：

1. 结果汇总模块
2. 最终结论生成
3. Markdown 报告导出
4. 对应状态推进与最小测试

目标是让链路真正实现：

`aggregating -> completed`

### 本轮范围

优先收口以下内容：

1. 将风险点、证据片段、规则依据汇总为统一结果对象
2. 生成面向审核人员查看的最终结论
3. 生成一份标准 Markdown 审查报告文件
4. 为任务补齐结果产物路径或结果引用信息
5. 补齐最小测试，验证结果生成链路可跑通

### 执行要求

1. 先阅读 `docs/tasks/current.md`
2. 先阅读 `docs/status/summary.md`
3. 结合现有 `docs/tech/v1/` 文档，补一版结果汇总阶段的技术文档或实现准备文档
4. 再直接落库本轮最小实现代码与测试
5. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
6. 完成后更新 `docs/tasks/current.md`
7. 如有必要，同步更新 `docs/tasks/t-now.md` 或相关短状态文档
8. 执行 `make check`
9. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议
这轮仍然优先保证最小闭环，不要提前扩展前端展示、审批流、复杂评分解释或规则编辑能力。

### 验收关注点

1. 结果对象与中间对象之间关系是否一致
2. 最终结论是否来自可追溯的风险点汇总
3. Markdown 报告是否可稳定导出
4. 任务状态是否能稳定推进到 `completed`

### 期望回复
请直接开始推进本轮实现，并将结果写回仓库，完成后等待总负责人复审。

### 期望回复
请完成这轮收口后，再继续推进审查输入装配和审查执行实现。

## 2026-03-28 18:00 总负责人 -> 技术负责人 T

### 主题
进入审查输入装配与审查执行阶段

### 背景
总负责人已完成对你最新一轮“解析能力与规则包格式口径收口”结果的复审。当前输入链路最小骨架已经可接受，解析占位实现和规则包文件格式口径也已明确。

### 审阅结论
当前阶段可以正式进入下一步：

1. 审查输入装配
2. 审查执行最小实现

### 本轮目标

请优先推进以下内容：

1. 基于解析结果对象装配审查输入
2. 基于规则包和固定任务指令加载运行时审查输入对象
3. 落一个最小审查执行骨架
4. 产出结构化风险点、证据片段和风险说明的最小 schema
5. 为后续结果汇总阶段准备可消费的中间对象

### 本轮范围

本轮优先收口：

1. 审查输入装配器
2. 规则包 + 固定任务指令的运行时组装
3. 审查执行最小占位实现
4. 风险点 / 证据片段 / 风险说明对象输出
5. 对应状态推进与最小测试

### 执行要求

1. 先阅读 `docs/tasks/current.md`
2. 先阅读 `docs/tech/v1/` 当前相关文档
3. 直接落库本轮技术文档和实现
4. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
5. 完成后更新 `docs/tasks/current.md`
6. 执行 `make check`
7. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议
这一轮仍然优先做“最小可跑通的审查链路”，不要提前展开结果汇总优化、前端展示扩展或复杂规则调优。

### 期望回复
请完成本轮产出后，再回到总负责人处接受复审。
