# 发给技术负责人 T 的收件箱

## 使用规则

- 本文件只保留当前唯一有效消息
- 历史消息统一移入 `docs/comms/archive/t-history.md`
- 当前消息处理完成后，要同步更新技术文档与 `docs/tasks/current.md`

## 当前唯一有效消息

## 2026-03-30 12:55 总负责人 -> 技术负责人 T

### 主题
进入下一轮唯一主题：基于审查对象的动态规则映射首包

### 审阅结论

当前正式结论如下：

1. 当前 `app/` 不需要推倒重来，现有文本提取、基础切块、`LLM` 收缩执行和结果链路都继续保留
2. 当前“政府采购审查对象解析扩展”第三轮通过，已开始具备八类政府采购审查对象入口
3. 当前正式拍板：下一轮唯一主题切换为“基于审查对象的动态规则映射首包”
4. 当前不新建平行实现目录，后续实现统一继续落在 `app/`
5. 当前不回头继续补页面、零散稳定性补丁、旧的 `completed` 验证主题或继续堆对象标签，先把对象真正映射到规则子集

### 本轮目标

请基于当前已识别的政府采购审查对象，在 `app/` 内落第一版“对象 -> 规则子集”动态规则映射，让执行器不要再让所有片段机械扫整包规则，而是优先只消费当前对象对应的规则子集。

### 本轮范围

当前范围如下：

1. 保留现有文本提取能力，不回退 `PDF` / `DOCX` / `DOC` 最小解析入口
2. 保留现有基础切块能力，不删除已有 `section / clause / paragraph` 骨架
3. 以 `docs/business/government-procurement-review-framework.md` 为准，先整理“审查对象 -> 一级规则域 -> 规则编码”的最小映射表
4. 映射范围至少覆盖：
- 资格条件
- 资格性审查表
- 符合性审查表
- 评分项 / 评分表
- 技术参数项
- 商务要求项
- 合同条款项
- 政策适用条款
- 不确定审查对象
5. 让现有执行器优先按该映射裁剪规则，不再默认让无关对象反复消费整包规则
6. `不确定审查对象` 保持兜底口径，可映射到较宽但仍受控的一组规则，不要退回全文扫整包规则
7. 必须保持现有 `LLM` 收缩约束不被破坏，不能因为规则映射而重新放大单批次规则载荷
8. 必须以真实样例 `SZCG2025000300-A` 作为主校准样例，验证对象映射后执行链路仍可运行
9. 保持 V1 极简边界，不扩页面、不扩多文件、不扩复杂运营能力
10. 如遇需要继续拍板的问题，再写入 `docs/comms/inbox-owner.md`

### 执行要求

1. 先阅读 `docs/status/summary.md`
2. 再阅读 `docs/tasks/t-now.md`
3. 再阅读 `docs/business/government-procurement-review-framework.md`
4. 再阅读 `docs/business/government-procurement-compliance-ontology.md`
5. 再阅读 `docs/tech/v1/government-procurement-review-object-round-3.md`
6. 必要时再阅读 `docs/tasks/current.md`
7. 只做“基于审查对象的动态规则映射首包”，不要回头继续补提示词规则、页面、结果层补丁或旧的 `completed` 验证主题
8. 默认按“单主题整包回提”方式推进，不再拆过细零散小包
9. 如有待拍板问题，写入 `docs/comms/inbox-owner.md`
10. 完成后更新 `docs/tasks/current.md`
11. 执行 `make check`
12. 自动 `commit` 和 `push`，提交信息使用中文

### 当前建议

- 当前先不要继续补页面、交付、工作台或复杂交互
- 当前不要回到“连通性修复”旧主题
- 当前不要回头继续拉长提示词与规则包范围
- 当前不要继续在结果层堆补丁
- 当前不要把主题做成“大而全”的完整策略引擎
- 当前优先把“政府采购审查对象 -> 规则子集”做成受控映射，再让现有 `LLM` 收缩链路消费

### 验收关注点

1. 八类政府采购审查对象与 `不确定审查对象` 是否都有受控规则子集映射
2. 执行器是否已优先按对象裁剪规则，而不是默认整包规则反复透传
3. 真实样例 `SZCG2025000300-A` 下，映射后执行链路是否仍可运行
4. 当前实现是否仍保持 V1 极简边界
5. 当前是否没有破坏既有文本提取、基础切块和自动化测试
