# 技术负责人 T 当前任务

## 当前阶段

- `LLM V1` 首轮验收验证已发现阻塞性交付缺口，当前暂停稳定性修复，先执行收缩方案

## 当前唯一目标

- 在不扩 V1 范围的前提下，先把 `LLM` 从重同步执行方式收缩到可验证版本，为后续稳定性与验收重新收口

## 当前要看什么

1. `docs/status/summary.md`
2. `docs/comms/inbox-t.md`
3. `docs/tech/v1/llm-v1-reduction-plan.md`
4. 必要时再读 `docs/tasks/current.md`
5. 需要复盘时再读 `docs/tasks/archive/`

## 当前默认动作

- 保持 V1 极简边界
- 当前代码侧已完成：
- `1. LLM` 配置读取与最小客户端
- `2. LLM` 审查执行器替换
- `3. 结果汇总链路接轨`
- 当前真实联调状态：
- 新配置下 `/models` 可通，极小 `chat/completions` 请求可回包
- 当前已确认新模型需通过 `OPENAI_DISABLE_THINKING=true` 关闭 thinking
- 当前已补 `message.content` / `message.reasoning` 兼容
- `SZCG2025000300-A` 已真实跑到 `completed`
- 当前结果页与下载链路已在真实调用下验证通过
- 当前已完成 `SZCG2025000300-A` 结果质量复盘
- 当前已收口下一轮建议：
- `1. 提示词与规则包联合收口`
- `2. 结果归并轻收口`
- `3. 长条款细切分与重复块降权`
- 当前已完成第一优先级“提示词与规则包联合收口”首包：
- 已补高价值规则优先提示词
- 已补重点规则命中定义、正反例和关注项
- 已补执行器整规则对象透传
- 已完成 `SZCG2025000300-A` 真实样例复核
- 当前已确认首包收益：
- `R5`、`R3`、`R1` 等高价值规则召回提升
- 当前已确认首包残留问题：
- 重复命中明显增加
- 结果数量膨胀明显
- 这些问题已正式切入下一主题
- 当前已完成第二优先级“结果归并轻收口”首包：
- 已补结果层轻量风险组归并
- 已让总体结论优先基于风险组数量
- 已让结果页与 Markdown 报告切到风险组视角
- 已完成 `SZCG2025000300-A` 真实样例联调
- 当前已确认首包收益：
- 结果口径已收口到风险组视角
- 显性重复命中有一定压降
- 当前已确认首包残留问题：
- 数量压降幅度有限
- 更深层重复仍指向长条款细切分与重复块问题
- 当前已正式进入第三优先级“长条款细切分与重复块降权”：
- 目标是压降长条款拆分重复、相邻片段重复和正文/偏离表平行区域重复
- 本轮优先从切分策略与重复块降权入手，不继续在结果层追加补丁
- 当前已完成第三优先级“长条款细切分与重复块降权”首包：
- 已补长条款细切分
- 已补重复块抑制与空白差异去重
- 已完成 `SZCG2025000300-A` 真实样例联调
- 当前已确认首包收益：
- 原始命中总数由 `18` 降到 `9`
- 风险组总数由 `17` 降到 `9`
- 高价值规则召回仍然保留
- 当前已确认首包结论：
- 当前无新的阻塞性交付缺口
- 当前精度优化主线已阶段性收口
- 当前下一步切入 `LLM V1` 验收验证：
- 重点核对上传页、等待页、结果页、Markdown 下载与真实文件结果可读性
- 重点确认当前版本是否可作为一版正式验证成果交付出来
- 当前已完成首轮验收验证正式记录：
- 文档见 `docs/tech/v1/llm-v1-acceptance-validation-round-1.md`
- 当前验收结果更新为：
- 真实样例 `SZCG2025000300-A` 未进入 `completed`
- 补充正常样例 `DOCX` / `DOC` 同样未进入 `completed`
- 当前失败根因已定位为：审查执行阶段 `LLM 调用失败，HTTP 504`
- 当前页面侧确认：
- `reviewing`、`failed`、上传拒绝页可用
- `not_found` 页反馈正确
- 当前正式结论：
- 本轮验收不通过，存在阻塞性交付缺口
- 当前已通过 `docs/comms/inbox-owner.md` 提交拍板请求
- 当前已调整主线为“LLM V1 收缩方案执行”：
- 先暂停稳定性修复线程，不继续围绕 `504` 做局部补丁
- 优先把 `LLM` 收缩为片段级判断器，推动轻量分批、缩批、最小重试与代码侧汇总边界收口
- 当前目标不是继续堆修补，而是先形成一版更轻、更稳的可验证执行方案
- 平时优先维护本文件，完成一轮实现并准备回提时再更新 `docs/tasks/current.md`
- 当前不继续扩页面、不补零散优化
- 当前不继续回头补提示词规则
- 当前不继续回头补第二优先级结果层补丁
