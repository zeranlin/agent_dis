# 当前任务

## 当前主题

- 第二份真实样例阶段收口已完成，等待下一轮唯一主题

## 当前阶段

- 已完成 `LLM V1` 收缩首包、收缩补充、业务模块优先筛选、政府采购审查对象解析扩展第三轮、样例候选送审链路收口首包、`SZCG2025000300-A` 受控真实复跑与结果复核、`BACG2025000066-A` 真实泛化验证第一轮，以及 `BACG2025000066-A` 结果复核与报告口径收口第一轮；当前已确认第二份真实样例达到阶段性收口，等待下一轮唯一主题

## 当前目标

- 在不扩 `V1` 范围的前提下，基于前两份真实样例阶段结果，判断下一轮主线应切入哪一个唯一主题

## 当前结论

- 当前不新建平行实现目录，后续实现统一继续落在 `app/`
- 当前不是推倒解析链路重来，而是在保留现有文本提取能力和基础切块能力的前提下，向上补业务化识别层
- 当前新增要稳定支持的业务单元为：
- `1. 资格条件`
- `2. 资格性审查表`
- `3. 符合性审查表`
- `4. 评分项 / 评分表`
- `5. 技术参数项`
- `6. 商务要求项`
- `7. 合同条款项`
- `8. 政策适用条款`
- 当前新增落实要求为：
- 保留现有文本提取能力
- 保留现有基础切块能力
- 往上补模块识别
- 往上补表格识别
- 往上补业务单元切分
- 往上补不确定区补判
- V1 产品边界已收敛为：单文件、`PDF` / `Word`、公开招标、货物与服务、报告驱动查看
- V1 主流程已收敛为：文件进入系统 -> AI 发起审查 -> 生成审查结果 -> 人工查看结果
- V1 最小闭环已具备上传、解析、审查执行、结果汇总、结果页和下载能力
- `LLM V1` 首轮真实闭环已打通
- 已完成 `SZCG2025000300-A` 的真实结果质量复盘
- 当前主要质量问题已收口为：高价值风险漏报、规则标题错位、同条款重复命中
- 当前问题归类已收口为：提示词问题、规则包问题、结构切分问题、结果归并与报告问题
- 当前优先级已收口为：
- `1. 提示词与规则包联合收口`
- `2. 结果归并轻收口`
- `3. 长条款细切分与重复块降权`
- 当前第一优先级“提示词与规则包联合收口”已完成首包验证
- 当前第二优先级“结果归并轻收口”已完成首包实现与真实联调
- 当前结果归并主题已完成的内容包括：
- 同条款同规则的显性重复命中轻量归并
- 总体结论优先基于风险组数量
- 结果页与 Markdown 报告切换为风险组视角
- 当前真实联调结果显示：
- 原始命中总数：`18`
- 归并后风险组总数：`17`
- 当前正式判断为：
- 第二优先级主题已通过首包验证
- 当前不存在新的阻塞性交付缺口
- 若继续追求明显的重复压降，应进入第三优先级主题，而不是继续在结果层加补丁
- 当前正式拍板进入第三优先级“长条款细切分与重复块降权”
- 当前第三优先级目标聚焦于：
- 长条款拆分重复压降
- 相邻重叠片段抑制
- 正文与偏离表平行区域重复降权
- 当前第三优先级“长条款细切分与重复块降权”已完成首包实现与真实联调
- 当前第三优先级主题已完成的内容包括：
- 进入审查前的长条款细切分
- 完全重复块抑制与正文/偏离表空白差异去重
- 更短的定位锚点与子片段定位后缀
- 当前真实联调结果进一步更新为：
- 审查输入条款片段数：`733`
- 原始命中总数：`9`
- 风险组总数：`9`
- 当前正式判断为：
- 第三优先级主题首包通过
- 当前不存在新的阻塞性交付缺口
- 当前精度优化主线已阶段性收口
- 当前正式转入 `LLM V1` 验收验证阶段
- 当前下一步聚焦于：
- 真实文件验证
- 页面闭环验证
- 报告下载验证
- 阻塞性交付缺口判断
- 当前已完成首轮验收验证并形成正式记录
- 当前真实验收结果显示：
- 真实样例 `SZCG2025000300-A` 未能进入 `completed`
- 补充正常样例 `DOCX` / `DOC` 同样未能进入 `completed`
- 当前真实阻塞集中在审查执行阶段
- 当前任务持久化记录中的核心报错为：`LLM 调用失败，HTTP 504`
- 当前失败页、上传拒绝页与 `not_found` 页反馈仍然正确
- 当前正式判断为：
- 本轮验收结论：不通过，存在阻塞性交付缺口
- 当前版本暂不能作为“当前阶段成果验证”放行
- 当前已完成“LLM V1 收缩方案执行”首包实现，并形成稳定性修复首包记录
- 当前已完成的稳定性修复内容包括：
- 请求载荷紧凑化
- `batch_char_budget` 字符预算切批
- 单批次失败拆半重试
- 单条款失败时继续缩短 `clause_max_chars`
- 当前自动化验证结果为：
- 新增稳定性相关测试已通过
- `make check` 已通过
- 当前真实样例批次级联调结果为：
- `SZCG2025000300-A` 共执行 `93` 个批次
- `93` 个批次全部成功
- 汇总命中数为 `16`
- 总耗时约 `946.68s`
- 当前已确认：
- 真实超时批次可被自动拆批化解
- 代码级稳定性阻塞已明显缓解
- 当前仍需明确保留的事实为：
- 本轮未继续等待完整“上传 -> completed -> 结果页 -> 下载”终态复跑
- 因此当前不能把结论写成“completed 验证已完成”
- 当前更准确的正式判断为：
- 稳定性修复首包已完成
- 已具备恢复进入“completed 验证”主题的条件
- 当前主线顺序正式固定为：
- `1. 先执行 LLM 收缩`
- `2. 再做 completed 验证`
- `3. 最后再做业务化解析优化`
- 当前不得跳过第二步直接进入解析优化
- 当前根据最新直接指令，第二步 `completed` 验证暂缓
- 当前已完成新一轮 `LLM` 收缩补充：
- 默认 `batch_size` 由 `8` 收紧到 `4`
- 默认 `clause_max_chars` 由 `1200` 收紧到 `800`
- 默认 `batch_char_budget` 上限由 `1400` 收紧到 `1000`
- 新增 `OPENAI_REVIEW_RULE_LIMIT`，默认每批最多携带 `6` 条规则
- 当前已补按批次筛选规则：
- 高价值规则优先保留
- 再按批次文本轻量匹配补充规则
- 不再默认把整包规则重复发送给每个批次
- 当前已补候选片段筛选：
- 高价值章节优先
- 条款片段优先
- 与规则关键词轻量匹配的片段优先
- 新增送审片段上限，默认最多 `160` 个候选片段
- 当前已补业务模块优先筛选：
- 资格条件
- 采购需求
- 评分办法
- 合同条款
- 程序条款
- 政策条款
- 当前已补规则级候选片段预筛：
- 每条规则先拿自己的候选片段池
- 每条规则默认最多保留 `24` 个候选片段
- 批次仅保留在当前批次中确有候选片段的规则
- 当前已开始落实业务化解析要求：
- 解析结果已显式保留 `module_type`
- 解析结果已显式保留 `unit_type`
- 采购需求表格行可进入 `参数项`
- 评分办法表格行可进入 `评分项`
- 偏离表表格行可进入 `偏离项`
- 长条款子片段会显式保留 `parent_unit_id`
- 当前已完成业务化解析第二轮：
- 解析结果已显式保留 `review_unit_id`
- 解析结果已显式保留 `unit_label`
- 解析结果已显式保留 `unit_name`
- 资格条件已进入 `单条资格要求 / 单条业绩要求 / 单条资质要求 / 单条信用要求`
- 采购需求已进入 `单条技术参数 / 单个参数项 / 单条功能要求 / 单条服务要求 / 单条交付要求`
- 评分办法已进入 `单个评分项 / 价格分规则 / 商务分规则 / 技术分规则`
- 合同条款已进入 `付款条款 / 验收条款 / 违约责任条款 / 质保条款 / 费用承担条款`
- 当前已完成政府采购审查对象解析扩展第三轮：
- 已补 `资格性审查表`
- 已补 `符合性审查表`
- 已补 `商务要求项`
- 已补 `政策适用条款`
- 已补 `不确定审查对象`
- 已补最小“近邻上下文”透传，支持表格行继承前置标题
- 当前已补载荷字段收缩：
- 去掉任务级字段
- 去掉条款级 `location_label`
- 不再继续透传 `negative_examples`
- 当前自动化验证已确认：
- 收缩后执行器、结果聚合和 worker 闭环仍可通过
- 新增业务化解析首包验证已通过：
- 长条款子片段具备父单元归属
- 偏离表内容不再退化为普通参数项
- 新增业务化解析第二轮验证已通过：
- 高价值模块的更细业务单元入口可被稳定识别
- 参数表行可提取稳定 `unit_name`
- 执行输入 `payload` 与 `schema` 已消费业务单元入口字段
- 新增政府采购审查对象扩展第三轮验证已通过：
- `资格性审查表 / 符合性审查表 / 商务要求项 / 政策适用条款 / 不确定审查对象` 均有自动化验证
- 真实样例 `SZCG2025000300-A` 中已出现上述五类对象标签
- 当前更准确的正式判断为：
- 原始业务化解析要求已开始进入真实实现
- 当前主链路已从“技术切片”推进到“带业务模块和业务单元类型的审查单元”
- 当前已从“业务模块 + 粗粒度单元类型”推进到“高价值业务单元入口”
- 当前已从“四类高价值模块入口”继续推进到“八类政府采购审查对象入口”
- 当前仍属于 V1 最小业务化落地，不是完整三级对象体系
- 当前已完成 `SZCG2025000300-A` 候选送审链路收口首包：
- 已让规则候选排序显式消费 `unit_label`
- 已补高价值规则的业务单元优先软加分
- 已补 `普通条款 / 普通表格行 / 不确定审查对象` 的轻量降权
- 当前离线样例观察显示：
- `R3` 已更稳定拿到 `原厂保修 / 培训方案` 等商务要求片段
- `R5` 已更稳定拿到 `同品牌内窥镜主机 / 同品牌 30 度内窥镜` 等技术参数片段
- `R12` 已更稳定拿到 `检测验证 / 违约责任 / 免费保修期` 等合同风险片段
- `R9` 候选池已进一步收口到评分相关单元，但仍需真实结果复核
- 当前更准确的正式判断更新为：
- 当前已把“业务单元入口”真正接入规则候选排序
- 当前高价值风险片段进入现有审查链路的稳定性进一步提升
- 当前仍未发现新的阻塞性交付缺口
- 当前已完成 `SZCG2025000300-A` 受控真实 `LLM` 复跑与结果核对第一轮：
- 任务已稳定进入 `completed`
- 最终结果共 `8` 组风险，其中高风险 `4` 组、中风险 `4` 组
- `R5` 同品牌约束已稳定进入最终结果，覆盖 `1.5.2 / 1.5.3 / 1.5.4` 三处技术条款
- `R12` 检测费用承担与责任失衡已稳定进入最终结果
- `R3` 原厂保修与培训资源偏严已进入最终结果
- 风险点、规则依据、证据片段、位置和最终报告已基本可对齐
- 当前仍存在 `R3` 噪声命中 1 条，但不构成新的阻塞性交付缺口
- 当前更准确的正式判断更新为：
- 当前关键目标风险已经进入真实最终结果
- 当前结果已达到“可读、可复核、有风险点”的本轮目标
- 当前下一步不应回头扩新主题，而应先做结果复核与报告口径收口
- 当前已完成结果复核与报告口径收口第一轮：
- 风险组数量由 `8` 组收口到 `7` 组
- `R3` 落在 `不确定审查对象` 的 1 条噪声命中已从最终展示中移除
- 风险组展示已优先消费真实条款元数据
- 报告中的规则编号已统一为 `R12 / R5 / R3` 等人工可读口径
- 总体结论、报告说明和风险组顺序已进一步收口到更适合人工复核的表达
- 当前更准确的正式判断更新为：
- `7` 组风险的表达已更适合人工复核
- 关键风险未回退，`R3` 噪声已进一步收口
- 当前未发现新的阻塞性交付缺口
- 当前已完成 `BACG2025000066-A` 真实泛化验证第一轮：
- 已在第二份真实样例上完成受控真实 `LLM` 复跑
- 已在 `app/review_executor.py` 新增落库前 `rule_code` 轻量纠偏
- 已把“评分标准量化不足，存在自由裁量权过大风险”从误挂 `R12` 收口到 `R9`
- 已保住初验 / 终验付款责任失衡风险继续落在 `R12`
- 当前最终稳定复跑结果为：
- 任务状态：`completed`
- 原始风险条数：`3`
- 规则分布：`R9 = 1`，`R12 = 2`
- 当前更准确的正式判断更新为：
- 第二份真实样例已审出可读、可复核的有效风险
- 当前首要泛化缺口已完成最小收口
- 当前未发现新的阻塞性交付缺口
- 当前已正式放行进入 `BACG2025000066-A` 的结果复核与报告口径收口
- 当前已完成 `BACG2025000066-A` 结果复核与报告口径收口第一轮：
- 当前最终结果稳定保持为 `3` 条高风险，规则分布为 `R9 = 1`、`R12 = 2`
- `R12` 风险标题已收口为“付款条件与财政资金挂钩，存在回款责任失衡风险”
- `R9` 风险标题已收口为“评分标准量化不足，自由裁量空间较大”
- 超长章节标题与命中位置已做展示级缩短
- 总体结论已改为基于真实风险焦点生成
- 当前结果页与 Markdown 报告口径已保持一致
- 当前更准确的正式判断更新为：
- 第二份真实样例已达到阶段性收口条件
- 当前继续停留在本样例微调主题的收益开始下降
- 当前可以将 `BACG2025000066-A` 作为第二份真实样例阶段成果正式收口

## 当前任务列表

### 总负责人

- 判断下一轮唯一主题是否切入第三份真实样例验证
- 保持当前主线不发散

### 产品经理 P

- 默认不介入主线推进，仅在需要时补业务口径
- 已补一版默认规则包的产品讨论视角重排，便于按资格 / 商务 / 技术 / 评分 / 合同五类统一讨论规则范围

### 技术负责人 T

- 当前最新已完成主题为：`BACG2025000066-A` 结果复核与报告口径收口第一轮
- 当前等待总负责人下发下一轮唯一主题
- 已完成 `LLM V1` 稳定性修复首包记录
- 已完成真实样例批次级跑通验证
- 已完成 `LLM` 收缩补充首包实现
- 已完成业务化解析落实首包：
- 补 `module_type` / `unit_type` / `parent_unit_id`
- 补 `偏离项` 识别
- 已完成业务化解析落实第二轮：
- 补 `review_unit_id` / `unit_label` / `unit_name`
- 补四类高价值模块的稳定业务单元入口
- 补结果页与 Markdown 报告对业务单元入口的展示
- 已完成政府采购审查对象解析扩展第三轮：
- 补审查表、商务要求、政策条款和不确定区补判
- 补最小上下文继承能力
- 已完成样例候选送审链路收口首包：
- 补规则候选排序对 `unit_label` 的消费
- 补高价值规则的业务单元优先软加分
- 补泛单元标签的轻量降权
- 已完成 `BACG2025000066-A` 真实泛化验证第一轮：
- 补落库前 `finding.rule_code` 轻量纠偏
- 补“评分语义误挂 `R12`”定向回归测试
- 已确认第二份真实样例当前可稳定进入 `completed`
- 已确认当前有效风险分布收口为 `R9 = 1`、`R12 = 2`
- 已完成受控真实 `LLM` 复跑与结果核对第一轮：
- 核对 `R5` 同品牌约束已进入最终结果
- 核对 `R12` 检测费用承担 / 责任失衡已进入最终结果
- 核对 `R3` 原厂保修 / 培训资源偏严已进入最终结果
- 核对最终报告、证据片段与位置字段基本对齐
- 已完成结果复核与报告口径收口第一轮：
- 风险组展示已优先消费 `ClauseRecord` 元数据
- 已补规则编号展示口径统一
- 已压掉 `R3` 的 1 条不确定对象噪声命中
- 已收口总体结论与报告说明文案
- 当前正式判断更新为：
- `SZCG2025000300-A` 已达到阶段性收口条件
- 当前下一步不再继续围绕单一样例深抠，而应切入第二份真实文件 `BACG2025000066-A` 验证泛化能力
- 当前需在 `app/` 内继续保留现有文本提取和基础切块，不新起平行实现目录
- 当前保持 V1 最小边界，不扩为完整三级对象体系，但要把输入骨架提升到更贴近真实审查对象

## 下一步建议

- 继续保持单主题整包推进
- 下一步优先对 `BACG2025000066-A` 做一次受控真实复跑
- 下一步重点核对第二份真实文件中的关键风险是否仍可进入最终结果
- 若第二份样例出现明显漏报或错位，再回到同主题内做泛化修补
- 如无新的阻塞性交付缺口，可逐步转入验收视角复核
- 若无新的阻塞性交付缺口，不建议回头拉长实现主题
- 当前不建议直接扩成完整三级对象体系
- 当前不建议回头继续补页面、规则大扩张或新的零散 `LLM` 补丁
- 如需复盘长历史，转读 `docs/tasks/archive/current-history-2026-03-30.md`

## 阻塞或待确认

- 当前未发现新的代码级阻塞性交付缺口
- 当前主要问题不是功能缺口，而是 `LLM` 执行成本与耗时仍偏高

## 当前交接主题

- `BACG2025000066-A` 的真实泛化验证

## 关联文档

- `docs/status/summary.md`
- `docs/tasks/owner-now.md`
- `docs/tasks/p-now.md`
- `docs/tasks/t-now.md`
- `docs/comms/inbox-owner.md`
- `docs/comms/inbox-t.md`
- `docs/tech/v1/progress-summary.md`
- `docs/tech/v1/llm-v1-implementation-round-2.md`
- `docs/tech/v1/sample-quality-review-szcg2025000300-a.md`
- `docs/tech/v1/prompt-and-rule-pack-round-1.md`
- `docs/tech/v1/result-aggregation-round-1.md`
- `docs/tech/v1/long-clause-splitting-round-1.md`
- `docs/tech/v1/llm-v1-acceptance-validation-round-1.md`
- `docs/tech/v1/llm-v1-reduction-plan.md`
- `docs/tech/v1/llm-v1-stability-fix-round-1.md`
- `docs/tech/v1/llm-v1-reduction-execution-round-1.md`
- `docs/tech/v1/business-oriented-parsing-execution-round-1.md`
- `docs/tech/v1/sample-risk-candidate-tuning-round-1.md`
- `docs/business/v1/rule-pack-by-business-module.md`
- `docs/tech/v1/sample-controlled-rerun-round-1.md`
- `docs/tech/v1/sample-report-polish-round-1.md`
- `docs/tasks/archive/current-history-2026-03-30.md`

## 历史归档

- 早期历史见 `docs/tasks/archive/current-history-2026-03-28.md`
- 本轮前后的详细历史见 `docs/tasks/archive/current-history-2026-03-30.md`
