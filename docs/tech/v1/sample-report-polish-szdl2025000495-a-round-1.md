# `SZDL2025000495-A` 结果复核与报告口径收口第一轮

## 文档目的

本文件记录第 3 份真实样例 `SZDL2025000495-A` 在“结果复核与报告口径收口”主题下的第一轮正式收口结果。

本轮只回答以下问题：

1. 当前 `8` 组风险的标题、规则归属和业务单元表达是否更适合人工复核
2. 当前证据片段、位置和风险说明是否基本可对齐
3. Markdown 报告与结果页口径是否已进一步一致
4. 当前是否已经达到“等待总负责人判断是否阶段性收口”的状态

## 本轮前提

本轮建立在以下已确认事实之上：

1. `SZDL2025000495-A` 真实泛化验证第一轮已通过
2. 任务当前可稳定进入 `completed`
3. 本轮不回头扩解析、页面、提示词大扩张或复杂策略
4. 本轮只允许在结果展示层做最小修补

## 本轮处理内容

### 一、`R12` 业务单元标签按合同语义细化

本轮在结果展示层新增最小语义归一逻辑，按风险标题和证据片段对 `R12` 的业务单元标签做轻量纠偏。

当前已能把原本偏粗的合同风险标签细化为：

- `付款条款`
- `单条合同条款`
- `验收条款`
- `违约责任条款`

该处理只影响结果展示口径，不回写解析主链路，也不扩新流程。

### 二、`R9` 的评分项标签恢复到人工可读口径

本轮补充了 `R9` 在 `不确定审查对象` 下的最小纠偏逻辑。

当前“售后服务及培训方案评价”类风险已可显示为：

- `单个评分项（售后服务及培训方案评价）`

不再继续以 `不确定审查对象` 口径对外展示。

### 三、补充针对性自动化验证

本轮新增并通过以下结果层回归测试：

1. `test_group_risks_refines_uncertain_r9_unit_label`
2. `test_group_risks_refines_r12_unit_label_by_contract_semantics`
3. `test_group_risks_distinguishes_contract_risk_titles_by_evidence`

## 当前离线复核结果

基于真实运行快照，当前归并后共保留 `8` 组风险，复核口径如下：

1. `付款条件与财政资金挂钩，存在回款责任失衡风险`
2. `付款条件与财政资金挂钩，存在回款责任失衡风险`
3. `单方解除且免责，存在合同责任失衡风险`
4. `审定金额核减后责任单方加重，存在合同失衡风险`
5. `履约评价直接决定付款比例，存在责任失衡风险`
6. `检测或验收条款缺少关键时限，建议人工复核`
7. `评分标准量化不足，自由裁量空间较大`
8. `违约责任明显偏重，存在合同失衡风险`

对应业务单元标签已收口为：

1. `付款条款（第一条 服务内容和合同金额）`
2. `付款条款（第三条 初验阶段）`
3. `单条合同条款（2 、如因政策原因导致合同不能履行或履行无意义的，甲方有权单方解除合同，...）`
4. `付款条款（第一条 服务内容和合同金额）`
5. `付款条款（第十一条 供应商履约评价）`
6. `验收条款（1. 甲方收到乙方自测报告后 个工作日内，有权要求乙方配合甲方参照《深圳...）`
7. `单个评分项（售后服务及培训方案评价）`
8. `违约责任条款（12.如乙方（供应商） 在软件开发过程中， 出现包括但不限于： 逻辑错误...）`

## 收口判断

### 一、风险标题

当前判断：`已明显好于上一轮，可支持人工快速复核`

主要改善点：

1. 合同类风险不再全部挤成同一标题
2. `R12` 已能区分付款、验收、违约和单方解除等不同合同语义
3. `R9` 的评分项标题和业务单元归属已恢复自然

### 二、规则归属

当前判断：`当前结果可用，但仍存在结构性偏斜`

本轮未改动规则召回主链路，因此：

1. `R12` 仍然明显偏多
2. `R9` 当前只保留 `1` 组评分风险
3. 当前偏斜仍属于质量问题，但不是新的阻塞性交付缺口

### 三、证据片段与位置

当前判断：`已基本满足人工回看`

当前结果已能把风险标题、证据片段和章节位置基本对齐到合同条款或评分项级别，符合 `V1` 的近似定位边界。

### 四、报告口径

当前判断：`已与结果页展示进一步一致`

本轮调整后，结果页与 Markdown 报告均更偏向“风险组 + 业务单元”的人工复核视角，整体阅读顺序更顺。

## 本轮结论

本轮正式结论如下：

1. `SZDL2025000495-A` 的结果复核与报告口径收口第一轮已完成
2. `R12` 的合同类风险标题和业务单元标签已进一步细化，更适合人工复核
3. `R9` 的评分项展示已恢复到自然口径
4. 当前结果仍存在“`R12` 明显偏多”的质量偏斜，但暂未构成新的阻塞性交付缺口
5. 当前已具备提交总负责人判断“是否达到阶段性收口”的条件

## 剩余问题

本轮仍保留以下非阻塞问题：

1. 两条“付款条件与财政资金挂钩”风险仍然相近，后续是否继续上卷归并需要单独拍板
2. 个别合同条款 `unit_name` 仍偏长，当前先不在本主题继续扩处理
3. `R12` 偏多问题已被看清，但本轮不回到规则与候选链路继续深挖

## 下一步建议

建议本主题下一步按以下顺序推进：

1. 先由总负责人判断第 3 份真实样例是否达到阶段性收口
2. 若判断通过，则将 `SZDL2025000495-A` 作为第 3 份真实样例阶段成果收口
3. 若判断仍需继续优化，只在同主题内处理“付款风险组是否继续上卷归并”这一单点问题
