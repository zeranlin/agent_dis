# `SZCG2025000300-A` 首轮补强链路核查与受控复跑记录

## 文档目的

本文件记录 `SZCG2025000300-A` 在“规则补强第一轮实现”之后的主链路核查、旧噪声压制和受控复跑结论。

本轮不进入第二轮能力补强，只回答以下问题：

1. 首轮新增 `3` 组规则是否真的进入候选筛选、送审、结果落库和最终报告主链路
2. `risk.rule_code` 看起来全空的根因是什么
3. 品牌 / 型号旧噪声为什么会继续放量
4. 受控复跑后，人工 `19` 条埋点里到底新增覆盖了哪些

## 核查范围

本轮继续保留以下既有能力，不回头扩新主题：

1. 文本提取
2. 基础切块
3. 业务单元识别
4. 候选送审筛选
5. `LLM` 收缩执行
6. 风险落库、结果汇总和 Markdown 报告输出

本轮代码改动只落在：

1. [app/repository.py](../../app/repository.py)
2. [app/review_executor.py](../../app/review_executor.py)
3. [tests/test_parse_worker.py](../../tests/test_parse_worker.py)

## 一、主链路核查结论

### 1. 首轮新增 `3` 组规则已经进入主链路

本轮核查确认，以下能力已经实际进入可执行代码路径：

1. `R3` 资格条件违法限定
2. `R4` 资格业绩区域 / 数量定向
3. `R9` 评分因素违法与价格分规则合法性

已确认进入的位置包括：

1. 候选筛选阶段：规则候选片段排序已消费 `module_type / unit_label / unit_name`
2. 送审阶段：批次规则选择会优先保留当前批次存在候选片段的规则
3. 审查阶段：启发式预命中与 `LLM` 命中在同一主链路内并流
4. 落库阶段：`rule_code` 会进一步归一化为已知规则并落到正式 `rule_id`
5. 报告阶段：最终结果页与 Markdown 报告继续消费最新结果对象

### 2. `rule_code` 全空并不是风险真的没有规则

本轮已查明，上一轮“`rule_code` 全空”的判断来自读到了同一 `task_id` 的旧结果，而不是最新风险对象真的丢了规则编号。

已确认的根因如下：

1. 同一任务多次生成结果时，`results.json` 会保留多份结果对象
2. 旧的 `get_result_by_task()` 只返回命中的第一份结果，不保证是最新结果
3. 老报告仍然是旧骨架格式，因此复核时会误以为新链路没有生效

当前已修正为：

1. 保存结果时，先清理同一 `task_id` 的旧结果
2. 读取结果时，按 `generated_at` 返回最新结果

这意味着：

1. `risk.rule_id` 实际并未整批丢失
2. 上一轮看到的旧标题、旧报告口径和旧高风险数量，根因是结果串读

## 二、旧噪声压制结论

### 1. `R5` 旧噪声已完成首轮压制

本轮已对以下典型旧噪声增加过滤：

1. 报价表里的 `规格/型号` 填写说明
2. 知识产权 / 商标权 / 专利权承诺
3. 验收场景中的普通 `型号` 核对

处理方式包括：

1. 候选排序阶段直接降为不可入选
2. 批次返回后再做一次噪声丢弃

### 2. `R4` 误报已补首轮压制

在受控复跑第二轮里，`R4` 已开始进入真实最终结果，但同时暴露出明显误报，主要落在：

1. `信用信息管理办法`
2. `评审委员会` 类程序说明
3. `特别警示条款`

为避免这些程序性条款继续挤占资格违法限定信号，本轮又追加了最小收口：

1. 对 `R4` 增加噪声位置识别
2. 当条款缺少真实业绩 / 案例 / 同类项目信号时，程序说明、信用承诺和特别警示条款不再进入 `R4`
3. 启发式预命中阶段同步避开此类误报

本轮只做了最小过滤，不扩复杂策略，不改变 `V1` 主流程。

## 三、受控复跑结果

## 第一次复跑

### 结果概况

1. 任务：`task_edfe40624c16`
2. 状态：`completed`
3. 归并后风险组：`7` 组
4. 关键结果：旧的 `135` 条高风险噪声结果已不再出现

### 已稳定进入最终结果的关键风险

1. `R5` 同品牌约束，覆盖 `1.5.2 / 1.5.3 / 1.5.4`
2. `R12` 检测费用承担 / 合同责任失衡
3. `R3` 原厂保修 / 培训资源偏严

### 本次结论

第一次复跑已经证明两件事：

1. 旧结果串读根因已被修正
2. `R5` 旧噪声主导结果的问题已明显缓解

## 第二次复跑

### 结果概况

1. 任务：`task_c4fd1223ea06`
2. 状态：`completed`
3. 原始风险条数：`14`
4. 风险组数：`13`

### 新增进入最终结果的覆盖

第二次复跑开始出现首轮补强里的新增覆盖：

1. `R9` 价格分计算方法疑似不符合低价优先原则
2. `R9` 评分因素设置不当，存在以无关证书资质打分风险
3. `R4` 资格业绩要求存在区域或数量定向风险

### 与人工 `19` 条埋点的新增覆盖对照

基于当前两次受控复跑，可确认本轮已经新增覆盖以下人工埋点方向：

1. `高新技术企业 / 科技型中小企业 / 纳税信用 A 级 / 成立满 X 年` 这一组资格违法限定入口已进入代码主链路
2. `资产总额 / 成立时间 / 无关证书资质打分` 这一组评分因素违法入口已进入真实结果
3. `价格分中间价 / 平均价 / 评标基准价` 这一组价格分规则入口已进入真实结果

当前仍未完全收口的点是：

1. `R4` 真实资格违法限定与程序说明误报仍有混杂
2. `R9` 个别命中位置的业务单元表达还不够自然

## 四、代码级落点

### [app/repository.py](../../app/repository.py)

1. `save_result()` 改为清理同任务旧结果后再写入
2. `get_result_by_task()` 改为返回最新结果

### [app/review_executor.py](../../app/review_executor.py)

1. 新增 `R5` 旧噪声识别与落库前过滤
2. 为 `R3 / R4 / R9` 增加规则特异信号加权
3. 对 `R9` 的通用评分程序说明条款做轻量降权
4. 放宽显式强信号在 `不确定审查对象` 中直通预命中
5. 新增 `R4` 程序说明 / 信用承诺 / 特别警示条款误报压制

### [tests/test_parse_worker.py](../../tests/test_parse_worker.py)

已补并通过以下回归：

1. 同任务结果读取返回最新结果
2. `R5` 报价说明 / 知识产权噪声过滤
3. `R5` 噪声不会继续落库
4. 显式资格 / 评分 / 价格分信号可在不确定单元中直通
5. `R9` 候选优先真实评分信号而不是通用程序说明
6. `R4` 信用办法 / 特别警示噪声过滤
7. `R4` 候选优先真实业绩限定条款而不是噪声上下文

## 五、本轮正式结论

本轮正式结论如下：

1. 首轮新增 `3` 组规则已经确认进入候选筛选、送审、落库和报告主链路
2. 上一轮“`rule_code` 全空”根因已经查清，核心问题是同任务多结果串读旧结果
3. `R5` 旧噪声已明显压降，旧的 `135` 条高风险骨架结果不再代表当前真实结果
4. 受控复跑已经开始体现首轮补强新增覆盖，`R9` 和 `R4` 不再完全缺位
5. 当前仍有 `R4` 局部误报与个别位置表达不自然问题，但尚不需要切入第二轮能力补强

## 六、下一步建议

下一步仍留在同主题内，只做最小收口：

1. 在当前 `R4` 噪声压制补丁基础上，执行一次更轻量的受控复跑核对
2. 重点确认真实资格违法限定条款是否已比程序说明条款更稳定进入前列
3. 如果上述核对成立，再由总负责人判断是否放行进入默认规则包 `1.1` 或下一轮能力补强
