# `SZDL2025000495-A` 真实泛化验证第一轮

## 文档目的

本文件记录第 3 份真实样例 `SZDL2025000495-A` 在当前收缩配置下的首轮真实泛化验证结果。

本轮只回答以下问题：

1. 当前链路是否还能稳定跑到 `completed`
2. 第 3 份真实文件是否能审出有价值风险，而不是空结果或明显失真结果
3. 当前结果是否存在新的泛化偏斜
4. 当前是否存在新的阻塞性交付缺口

## 本轮前提

本轮建立在以下已确认事实之上：

1. `SZCG2025000300-A` 已阶段性收口
2. `BACG2025000066-A` 已阶段性收口
3. `app/` 现有文本提取、基础切块、业务单元识别、候选送审、`LLM` 收缩执行和结果链路继续保留
4. 本轮不扩页面、不扩多文件、不进入复杂策略引擎

## 运行配置

本轮继续使用当前收缩配置，关键参数如下：

- `batch_size`: `4`
- `rule_limit`: `6`
- `max_clauses`: `160`
- `max_clauses_per_rule`: `24`
- `clause_max_chars`: `800`
- `batch_char_budget`: `1000`
- `disable_thinking`: `true`
- `timeout_seconds`: `1800`

说明：

- 真实 `LLM` 地址、模型和密钥全部只通过本地环境变量注入
- 敏感配置未写入仓库

## 执行结果

### 任务终态

- 任务状态：`completed`
- `parse_jobs = 1`
- `review_jobs = 1`
- `result_jobs = 1`
- `rounds = 2`

### 原始结果概况

- 原始风险条数：`10`
- 风险组数量：`8`
- 高风险组数量：`8`
- 中风险组数量：`0`
- 低风险组数量：`0`

### 当前规则分布

- `R12`: `9`
- `R9`: `1`

说明：

- 当前第 3 份样例的风险仍明显集中在合同条款相关问题
- 但本轮不是空结果，也不是全部明显失真结果

## 关键结果核对

### 一、任务稳定性

结论：`已稳定进入 completed`

当前链路在第 3 份真实样例上仍可完整跑通：

- 上传成功
- 解析成功
- 审查执行成功
- 结果汇总成功
- 最终结果可读取

### 二、风险有效性

结论：`已出现可读、可复核的有效风险`

当前可看到的风险重点包括：

1. 付款条件与财政资金挂钩
2. 审定金额核减后由乙方单方退差或补足
3. 单方解除且免责
4. 履约评价直接决定付款比例
5. 检测或验收条款缺少关键时限
6. 违约责任明显偏重
7. 评分标准量化不足，自由裁量空间较大

### 三、当前泛化偏斜

结论：`存在，但暂未到阻塞级别`

本轮最主要的偏斜有两类：

1. 风险高度集中在 `R12`
2. 个别合同类条款仍显示为偏粗的业务单元标签

其中本轮已在结果层做轻量修补，把多类合同风险标题拆开，不再全部显示为同一标题，当前可见标题包括：

- `付款条件与财政资金挂钩，存在回款责任失衡风险`
- `审定金额核减后责任单方加重，存在合同失衡风险`
- `单方解除且免责，存在合同责任失衡风险`
- `履约评价直接决定付款比例，存在责任失衡风险`
- `检测或验收条款缺少关键时限，建议人工复核`
- `违约责任明显偏重，存在合同失衡风险`

## 对齐度判断

基于当前第 3 份真实样例结果，当前判断如下：

### 1. 风险点

- 已出现多类可读风险
- 当前合同条款相关风险较集中

### 2. 规则依据

- `R12` 与 `R9` 已进入最终结果
- 当前未看到明显规则编号丢失

### 3. 证据片段

- 当前证据片段已能支持大部分风险标题
- 仍需后续继续复核是否存在应落其他规则但被压到 `R12` 的情况

### 4. 基本位置

- 已能回到章节与条款级位置
- 当前仍是近似位置，不是精确页码定位，符合 `V1` 边界

### 5. 最终报告

- 结果接口、总体结论和 Markdown 报告链路仍然可用
- 当前结论已收口为“合同付款条款和合同履约条款”为主要焦点

## 本轮结论

本轮正式结论如下：

1. `SZDL2025000495-A` 已完成一次受控真实 `LLM` 复跑，且任务稳定进入 `completed`
2. 第 3 份真实样例已审出有价值风险，不是空结果
3. 当前主要风险集中在 `R12` 合同条款相关问题，另有 `1` 条 `R9` 评分量化风险
4. 当前已在结果层把多类合同风险标题拆开，结果表达已明显好于“全部同标题”
5. 当前未发现新的阻塞性交付缺口

## 剩余问题

本轮仍保留以下非阻塞问题：

1. 第 3 份样例当前结果明显偏向 `R12`
2. 个别 `R12` 风险的业务单元标签仍偏粗
3. 是否还存在应进入其他规则但尚未显式进入最终结果的风险，仍需继续复核

## 下一步建议

建议按以下顺序推进：

1. 先将本轮 `SZDL2025000495-A` 真实泛化验证结果作为正式基线写回任务状态
2. 由总负责人判断是否先进入 `SZDL2025000495-A` 的结果复核与报告口径收口
3. 若继续优化，优先只在同主题内处理 `R12` 过于集中这一泛化偏斜，不扩新设计
