# `SZCG2025000300-A` 规则补强第一轮结果复核

## 文档目的

本文件用于正式复核 `SZCG2025000300-A` 人工差异导向规则补强第一轮的真实结果表现。

本次复核关注的问题不是“代码里是否已经做了首轮补强”，而是：

1. 首轮新增的 `3` 组能力是否已经在真实结果中体现出来
2. 与人工审核表相比，覆盖是否出现了明显提升
3. 是否可以放行进入第二轮能力补强

## 复核对象

### 人工审核基准

人工审核表共 `19` 条埋点，主要覆盖以下 `5` 类问题：

1. 资格条件违法限定
2. 评分因素 / 评分方法违法
3. 检测机构与检测标准合规
4. 技术参数区间与冲突检查
5. 价格分规则合法性

### 本轮系统结果

本轮复核使用的系统任务如下：

1. 任务 `task_7343b8b385e0`
2. 结果 `result_96c392657473`
3. 状态：`completed`
4. 结果时间：`2026-03-29 23:48:46`（东八区）

对应输出位于运行时目录：

1. 报告文件：`/tmp/agent_dis_runtime/outputs/reports/task_7343b8b385e0-审查报告.md`
2. 结果对象：`/tmp/agent_dis_runtime/metadata/results.json`
3. 风险对象：`/tmp/agent_dis_runtime/metadata/risks.json`

## 关键事实

基于本次结果对象直接复核，当前有以下明确事实：

1. 当前结果共落出 `135` 条高风险
2. `135` 条风险的 `rule_code` 全部为空
3. 报告总体结论仍是“本文件存在 `135` 条高风险问题”
4. 前部结果几乎被“品牌 / 型号指向检查”类噪声淹没
5. 命中位置大量落在报价表字段说明、合同说明、知识产权条款、常规型号描述等不应优先报警的位置

当前结果说明：

1. 首轮新增的 `资格条件违法限定`
2. 首轮新增的 `评分因素 / 评分方法违法`
3. 首轮新增的 `价格分规则合法性`

至少在这次真实结果里，没有以清晰、稳定、可复核的方式体现出来。

## 与人工审核表的差异判断

### 一、人工高价值埋点未在本轮结果中体现

针对人工表中的以下典型埋点，本轮结果未见稳定命中：

1. `科技型中小企业`
2. `高新技术企业证书`
3. `纳税信用 A 级证明`
4. `成立满 5 年以上`
5. `深圳市医疗器械行业同类项目业绩不少于 2 个`
6. `资产总额设分`
7. `从业人员设分`
8. `纳税额设分`
9. `成立年限设分`
10. `人力资源测评师`
11. `无关证书：非金属矿采矿许可证`
12. `价格分中间价优先法`

这意味着首轮 `3` 组补强虽然已经进入代码实现，但当前尚未在最新真实结果中兑现为可见覆盖提升。

### 二、旧噪声仍主导结果输出

本轮结果最突出的现象不是“新增能力生效”，而是旧噪声仍明显压制结果：

1. `R5` 语义的品牌 / 型号噪声仍大面积出现
2. 报价表里的 `品牌 / 规格 / 型号` 字段说明被误当作高风险
3. 合同或承诺函中的 `专利权 / 商标权` 等知识产权表述也被卷入误报
4. 同一位置存在重复命中，结果数量明显膨胀

这会直接掩盖新增规则型能力的真实表现，也会显著拉低人工复核效率。

### 三、当前结果不具备进入第二轮的条件

是否进入第二轮能力补强，关键前提应该是：

1. 第一轮新增能力已经真正进入主链路
2. 至少在 `300-A` 上已出现可证明的人工埋点覆盖提升
3. 旧噪声没有继续主导最终结果

当前这三个前提都没有被本轮结果证明。

因此，当前不应直接放行进入第二轮能力补强。

## 初步根因判断

基于本轮结果，当前更可能存在以下链路问题：

1. 首轮新增规则虽然进入了代码，但未稳定进入当前真实运行主链路
2. 结果落库或汇总阶段没有保留新增规则的 `rule_code`
3. 旧的品牌 / 型号噪声规则仍在大量放量输出，压制了新增能力的可见性
4. 当前最新运行结果，很可能没有体现“首轮补强后应有的结果形态”

这里需要特别保留一个判断：

当前不能直接下结论说“首轮补强实现无效”，但可以明确下结论说：

- 首轮补强尚未在最新真实结果中被验证出来。

## 正式结论

本轮正式结论如下：

1. `SZCG2025000300-A` 规则补强第一轮首包实现已经完成
2. 但基于最新真实结果复核，当前尚未证明这 `3` 组能力已经形成明显覆盖提升
3. 当前最新结果仍被品牌 / 型号类旧噪声主导
4. 当前 `rule_code` 全空，说明链路上仍存在明显异常
5. 当前不放行进入第二轮能力补强

## 下一步唯一建议

下一步不要继续扩第二轮专题，先回到第一轮做链路核查和受控复跑。

建议技术负责人 T 只做以下 `4` 件事：

1. 核查首轮新增 `3` 组规则是否真的进入候选筛选、送审、结果落库和最终报告主链路
2. 查清 `risk.rule_code` 全空的根因
3. 压制品牌 / 型号旧噪声在报价表说明、合同说明、知识产权条款等位置的误报
4. 基于 `300-A` 重新复跑，并明确回提“人工 `19` 条埋点中到底新增覆盖了哪些”
