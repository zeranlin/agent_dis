# LLM V1 验收验证首轮记录

## 1. 本轮基本信息

- 轮次名称：`LLM V1` 验收验证首轮
- 记录日期：`2026-03-29`
- 执行人：技术负责人 `T`
- 关联主题：`LLM V1` 验收验证
- 关联文档：
- `docs/status/summary.md`
- `docs/tasks/t-now.md`
- `docs/tasks/current.md`
- `docs/tech/v1/long-clause-splitting-round-1.md`

## 2. 本轮目标

本轮只回答以下问题：

1. 主流程是否稳定跑通
2. 失败路径是否反馈正确
3. 页面展示与下载结果是否一致
4. 是否存在阻塞性交付缺口

## 3. 联调样例记录

### 样例 1

- 样例名称：`[SZCG2025000300-A]香港中文大学（深圳）香港中文大学（深圳）教学培训用外科医疗机械人系统项目.docx`
- 样例类型：真实样例
- 文件格式：`DOCX`
- 预期结果：上传成功后进入 `completed`，结果页可读，可下载 `Markdown`
- 实际结果：上传页与等待页可正常进入，但任务在审查执行阶段失败，未进入 `completed`
- 任务状态推进：`uploaded -> waiting -> failed`
- 结果页表现：失败页可访问，显示“审查未完成”
- 下载结果表现：不可下载，未生成结果文件
- 是否通过：否
- 备注：任务持久化记录中的 `error_code` 为 `REVIEW_EXECUTION_FAILED`，`error_message` 为 `LLM 调用失败，HTTP 504`，说明当前真实 `LLM` 服务在审查执行阶段超时

### 样例 2

- 样例名称：`parser-test.docx`
- 样例类型：补充正常样例
- 文件格式：`DOCX`
- 预期结果：进入 `completed`，结果页可读，可下载 `Markdown`
- 实际结果：同样在审查执行阶段失败，未进入 `completed`
- 任务状态推进：`uploaded -> waiting -> failed`
- 结果页表现：失败页可访问
- 下载结果表现：不可下载，未生成结果文件
- 是否通过：否
- 备注：失败提示与真实样例一致，说明问题并非单一文件特例

### 样例 3

- 样例名称：`补充样例-常规DOC.doc`
- 样例类型：补充正常样例
- 文件格式：`DOC`
- 预期结果：进入 `completed`，结果页可读，可下载 `Markdown`
- 实际结果：同样在审查执行阶段失败，未进入 `completed`
- 任务状态推进：`uploaded -> waiting -> failed`
- 结果页表现：失败页可访问
- 下载结果表现：不可下载，未生成结果文件
- 是否通过：否
- 备注：正常 `DOCX` / `DOC` 样例均未跑通，当前不能认定主流程稳定

### 样例 4

- 样例名称：`失败样例-无可审查正文.docx`
- 样例类型：失败样例
- 文件格式：`DOCX`
- 预期结果：进入失败态，并给出“文件无可审查正文”反馈
- 实际结果：符合预期
- 任务状态推进：`uploaded -> waiting -> failed`
- 结果页表现：失败页可访问，失败提示清晰
- 下载结果表现：不适用
- 是否通过：是
- 备注：任务错误码为 `DOCUMENT_NO_REVIEWABLE_TEXT`

### 样例 5

- 样例名称：`失败样例-不支持格式.txt`
- 样例类型：失败样例
- 文件格式：`TXT`
- 预期结果：上传时直接拒绝，并提示仅支持 `PDF` 或 `Word`
- 实际结果：符合预期
- 任务状态推进：未创建任务，上传入口直接返回 `415`
- 结果页表现：不适用
- 下载结果表现：不适用
- 是否通过：是
- 备注：上传页错误提示为“仅支持 PDF 或 Word 文件。”

## 4. 页面走查记录

### `reviewing`

- 是否可正常访问：是
- 主要提示是否正确：是，页面包含“等待审查”“查看状态接口”“直接查看结果页”
- 主操作是否正确：是
- 是否通过：是

### `completed`

- 是否可正常访问：否，本轮正常样例均未在真实环境进入 `completed`
- 结论、风险、下载入口是否齐全：无法完成验收
- 页面与下载结果是否一致：无法完成验收
- 是否通过：否

### `failed`

- 是否可正常访问：是
- 失败提示是否正确：是，正常样例失败时提示“审查执行失败，请检查 LLM 配置或服务可用性。”；无正文样例提示“文件无可审查正文”
- 失败分类是否可理解：是，当前已验证 `REVIEW_EXECUTION_FAILED` 与 `DOCUMENT_NO_REVIEWABLE_TEXT`
- 是否通过：是

### `not_found`

- 是否可正常访问：是，返回 `404`
- 提示是否正确：是，页面标题为“未找到可查看的结果页”，并引导回上传入口
- 是否通过：是

## 5. 阻塞缺口判断

### 阻塞缺口 1

- 标题：真实 `LLM` 审查执行阶段无法稳定完成
- 影响范围：所有正常样例均无法进入 `completed`，导致结果页、风险组展示和 `Markdown` 下载无法完成真实验收
- 复现方式：使用当前本地环境变量配置上传真实样例 `SZCG2025000300-A` 或补充正常样例，任务均在审查执行阶段失败
- 当前判断：阻塞性交付缺口
- 是否需要回到实现修改：需要先由总负责人拍板；若继续由技术侧处理，优先方向应为缩小单次请求负载、增加重试或更换可用联调服务

如果没有阻塞缺口，明确写：

- 本轮未发现阻塞性交付缺口

## 6. 非阻塞问题

1. 当前失败页与上传拒绝页反馈总体清晰，可继续沿用
2. `reviewing` 与 `not_found` 页面文案已具备演示价值
3. 本轮主要问题集中在真实 `LLM` 执行稳定性，不在页面文案或失败态展示

## 7. 本轮结论

- 不通过，存在阻塞性交付缺口

补充说明：

- 当前阻塞并非页面闭环缺失，而是正常样例在真实 `LLM` 审查执行阶段被 `504 Gateway Time-out` 中断
- 在该阻塞解除前，不能给出“可进入当前阶段成果验证”的放行结论

## 8. 下一步建议

1. 由总负责人先确认是优先处理联调服务稳定性，还是由技术侧回到实现补最小重试 / 缩批策略
2. 在阻塞解除前，不继续追加新的精度优化主题
3. 阻塞解除后，再补一次同主题验收复跑，重点复核 `completed` 页面与 `Markdown` 下载
